Support Matrix
==============

.. This file is generated by tools/support_matrix.py. Do not edit by hand.

This matrix tracks CrossGL backend coverage from the checked-in support
catalog. A feature is marked supported only when the repo has implementation
and test evidence for the CrossGL contract. Unknown means unaudited, not
implicitly supported.

.. csv-table:: Status legend
   :header: "Code", "Status", "Meaning"
   :widths: 8, 20, 72

   "Y", "supported", "Implemented with positive tests for the repo-owned CrossGL contract."
   "P", "partial", "Implemented for common cases, but known target features or edge cases are missing."
   "D", "diagnostic", "The backend emits a deterministic diagnostic or fallback instead of full target code."
   "R", "validated_rejection", "The backend rejects this shape with an explicit validation error."
   "U", "unsupported", "The backend intentionally does not support this capability yet."
   "?", "unknown", "Not audited yet. Do not treat this as supported."

.. csv-table:: Backend inventory
   :header: "Backend", "Ext", "Target generator", "Source kind", "Native frontend", "Tests", "Test count", "Unsupported markers", "Docs source"

   "DirectX / HLSL", ".hlsl", "crosstl/translator/codegen/directx_codegen.py", "native", "crosstl/backend/DirectX", "tests/test_translator/test_codegen/test_directx_codegen.py, tests/test_backend/test_directx", "1071", "291", "Microsoft Learn HLSL reference; HLSL specification project"
   "OpenGL / GLSL", ".glsl", "crosstl/translator/codegen/GLSL_codegen.py", "native", "crosstl/backend/GLSL", "tests/test_translator/test_codegen/test_GLSL_codegen.py, tests/test_backend/test_GLSL", "1167", "177", "GLSL 4.60 specification; OpenGL registry"
   "Metal", ".metal", "crosstl/translator/codegen/metal_codegen.py", "native", "crosstl/backend/Metal", "tests/test_translator/test_codegen/test_metal_codegen.py, tests/test_backend/test_metal", "962", "492", "Apple Metal resources; Metal Shading Language specification"
   "Vulkan SPIR-V", ".spvasm", "crosstl/translator/codegen/SPIRV_codegen.py", "native", "crosstl/backend/SPIRV", "tests/test_translator/test_codegen/test_SPIRV_codegen.py, tests/test_backend/test_SPIRV", "975", "36", "SPIR-V unified grammar; Khronos SPIR-V headers"
   "CUDA", ".cu", "crosstl/translator/codegen/cuda_codegen.py", "native", "crosstl/backend/CUDA", "tests/test_translator/test_codegen/test_CUDA_codegen.py, tests/test_backend/test_CUDA", "778", "194", "CUDA C++ programming guide"
   "HIP", ".hip", "crosstl/translator/codegen/hip_codegen.py", "native", "crosstl/backend/HIP", "tests/test_translator/test_codegen/test_hip_codegen.py, tests/test_backend/test_HIP", "821", "136", "ROCm HIP documentation"
   "Mojo", ".mojo", "crosstl/translator/codegen/mojo_codegen.py", "native", "crosstl/backend/Mojo", "tests/test_translator/test_codegen/test_mojo_codegen.py, tests/test_backend/test_mojo", "921", "98", "Mojo manual"
   "Rust", ".rs", "crosstl/translator/codegen/rust_codegen.py", "native", "crosstl/backend/Rust", "tests/test_translator/test_codegen/test_rust_codegen.py, tests/test_backend/test_rust", "979", "69", "Rust reference"
   "Slang", ".slang", "crosstl/translator/codegen/slang_codegen.py", "native", "crosstl/backend/slang", "tests/test_translator/test_codegen/test_slang_codegen.py, tests/test_backend/test_slang", "786", "410", "Slang user guide"

.. csv-table:: Summary by backend
   :header: "Backend", "supported", "partial", "diagnostic", "validated_rejection", "unsupported", "unknown"

   "DirectX / HLSL", "49", "3", "2", "0", "0", "0"
   "OpenGL / GLSL", "49", "3", "2", "0", "0", "0"
   "Metal", "48", "3", "3", "0", "0", "0"
   "Vulkan SPIR-V", "49", "3", "2", "0", "0", "0"
   "CUDA", "44", "3", "7", "0", "0", "0"
   "HIP", "44", "3", "7", "0", "0", "0"
   "Mojo", "46", "3", "5", "0", "0", "0"
   "Rust", "47", "3", "4", "0", "0", "0"
   "Slang", "48", "3", "3", "0", "0", "0"

Graphics Backend Focus
----------------------

This view isolates the DirectX, OpenGL, and Metal rows that are in
scope for graphics backend completion work.

.. csv-table:: Graphics backend status summary
   :header: "Backend", "supported", "partial", "diagnostic", "validated_rejection", "unsupported", "unknown"

   "DirectX / HLSL", "49", "3", "2", "0", "0", "0"
   "OpenGL / GLSL", "49", "3", "2", "0", "0", "0"
   "Metal", "48", "3", "3", "0", "0", "0"

.. csv-table:: DirectX/OpenGL/Metal actionable backlog
   :header: "Backend", "Category", "Feature", "Status", "Notes"

   "DirectX / HLSL", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, validates current include-directory resolved paths with expected/actual include-directory status, resolved-path, and frontend-visibility validation context, rejects unexpected generated include-directory status fields, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, recursively scans #include directives into per-unit includeDependencies records, ignores block-commented preprocessor directives while still recognizing directives after same-line block comments with optional nested source paths while honoring simple #if, #ifdef, #ifndef, #elif, #else, and #endif branches with project and variant defines, including defined checks, boolean operators, parentheses, integer and boolean define values, and simple integer comparisons, local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes and byte sizes, source/include-dir resolution source, and project-define plus variant provenance for define-backed include targets, emits structured diagnostics for missing local, dynamic, outside-project, and cyclic include directives with project-define and variant provenance, including structured variant fields and diagnostic variant rollups for missing, dynamic, and cyclic include diagnostics when applicable, validates include dependency record shape including unexpected generated include dependency fields, summary rollups including source-backend, source-backend status, resolution-source, and variant counts, missing current include-scan diagnostics, missing include dependency summary rollups, missing and extra current-scan dependency records, current status, resolved path, resolved include hash, resolved include size, expected/actual include status, path, source, hash, and byte-size mismatch context, source/include-dir resolution source, project-define plus variant include provenance, and current-scan mismatch labels include resolved path, resolution source, and project-define provenance, applies CLI include-directory overrides with normalized repository-relative separators and rejects empty CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, resolves CrossGL quoted includes relative to the current source file and configured include paths, applies named-variant define maps and project include directories to OpenGL/GLSL translation with a resolved angle include and variant-specific conditional output, preserves unresolved CrossGL includes for reporting and downstream handling, rejects CrossGL include cycles, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, verifies include-path forwarding metadata for every registered source frontend, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, target, source backend, and variant, reports resolved include read failures with project.scan.include-read-failed diagnostics while preserving the already discovered include dependency, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including target and variant rollups, missing include-path processing target and variant rollups, and text inspection identifies inactive include directories by path, resolved path, and frontend visibility, include dependency status, kind, source-backend, source-backend status, resolution-source, variant, and include-path processing rollups by status, target, source backend, and variant, resolved include dependency samples, system include dependency samples, and unresolved include dependency samples with source-backend labels, project-define provenance, variant names, unit source hash and byte-size metadata, and resolved include hash and byte-size metadata, sampled include-path processing artifact metadata, and include-path processing inspection summaries include configured include-directory status records plus frontend-visible and inactive directory counts. It applies named-variant define maps and project include directories to DirectX/HLSL and Metal/MSL project translation with resolved local headers and variant-specific conditional output, Slang project translation with resolved local headers and variant-specific conditional output, Vulkan project translation with a resolved angle include and variant-specific conditional output, and to CUDA/HIP project translation with unresolved runtime system includes, resolved local headers, and variant-specific conditional output. Include semantics remain backend-dependent, so this remains partial project support. Resolved include dependency reports and inspection samples include byte-size metadata."
   "OpenGL / GLSL", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, validates current include-directory resolved paths with expected/actual include-directory status, resolved-path, and frontend-visibility validation context, rejects unexpected generated include-directory status fields, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, recursively scans #include directives into per-unit includeDependencies records, ignores block-commented preprocessor directives while still recognizing directives after same-line block comments with optional nested source paths while honoring simple #if, #ifdef, #ifndef, #elif, #else, and #endif branches with project and variant defines, including defined checks, boolean operators, parentheses, integer and boolean define values, and simple integer comparisons, local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes and byte sizes, source/include-dir resolution source, and project-define plus variant provenance for define-backed include targets, emits structured diagnostics for missing local, dynamic, outside-project, and cyclic include directives with project-define and variant provenance, including structured variant fields and diagnostic variant rollups for missing, dynamic, and cyclic include diagnostics when applicable, validates include dependency record shape including unexpected generated include dependency fields, summary rollups including source-backend, source-backend status, resolution-source, and variant counts, missing current include-scan diagnostics, missing include dependency summary rollups, missing and extra current-scan dependency records, current status, resolved path, resolved include hash, resolved include size, expected/actual include status, path, source, hash, and byte-size mismatch context, source/include-dir resolution source, project-define plus variant include provenance, and current-scan mismatch labels include resolved path, resolution source, and project-define provenance, applies CLI include-directory overrides with normalized repository-relative separators and rejects empty CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, resolves CrossGL quoted includes relative to the current source file and configured include paths, applies named-variant define maps and project include directories to OpenGL/GLSL translation with a resolved angle include and variant-specific conditional output, preserves unresolved CrossGL includes for reporting and downstream handling, rejects CrossGL include cycles, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, verifies include-path forwarding metadata for every registered source frontend, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, target, source backend, and variant, reports resolved include read failures with project.scan.include-read-failed diagnostics while preserving the already discovered include dependency, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including target and variant rollups, missing include-path processing target and variant rollups, and text inspection identifies inactive include directories by path, resolved path, and frontend visibility, include dependency status, kind, source-backend, source-backend status, resolution-source, variant, and include-path processing rollups by status, target, source backend, and variant, resolved include dependency samples, system include dependency samples, and unresolved include dependency samples with source-backend labels, project-define provenance, variant names, unit source hash and byte-size metadata, and resolved include hash and byte-size metadata, sampled include-path processing artifact metadata, and include-path processing inspection summaries include configured include-directory status records plus frontend-visible and inactive directory counts. It applies named-variant define maps and project include directories to DirectX/HLSL and Metal/MSL project translation with resolved local headers and variant-specific conditional output, Slang project translation with resolved local headers and variant-specific conditional output, Vulkan project translation with a resolved angle include and variant-specific conditional output, and to CUDA/HIP project translation with unresolved runtime system includes, resolved local headers, and variant-specific conditional output. Include semantics remain backend-dependent, so this remains partial project support. Resolved include dependency reports and inspection samples include byte-size metadata."
   "Metal", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, validates current include-directory resolved paths with expected/actual include-directory status, resolved-path, and frontend-visibility validation context, rejects unexpected generated include-directory status fields, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, recursively scans #include directives into per-unit includeDependencies records, ignores block-commented preprocessor directives while still recognizing directives after same-line block comments with optional nested source paths while honoring simple #if, #ifdef, #ifndef, #elif, #else, and #endif branches with project and variant defines, including defined checks, boolean operators, parentheses, integer and boolean define values, and simple integer comparisons, local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes and byte sizes, source/include-dir resolution source, and project-define plus variant provenance for define-backed include targets, emits structured diagnostics for missing local, dynamic, outside-project, and cyclic include directives with project-define and variant provenance, including structured variant fields and diagnostic variant rollups for missing, dynamic, and cyclic include diagnostics when applicable, validates include dependency record shape including unexpected generated include dependency fields, summary rollups including source-backend, source-backend status, resolution-source, and variant counts, missing current include-scan diagnostics, missing include dependency summary rollups, missing and extra current-scan dependency records, current status, resolved path, resolved include hash, resolved include size, expected/actual include status, path, source, hash, and byte-size mismatch context, source/include-dir resolution source, project-define plus variant include provenance, and current-scan mismatch labels include resolved path, resolution source, and project-define provenance, applies CLI include-directory overrides with normalized repository-relative separators and rejects empty CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, resolves CrossGL quoted includes relative to the current source file and configured include paths, applies named-variant define maps and project include directories to OpenGL/GLSL translation with a resolved angle include and variant-specific conditional output, preserves unresolved CrossGL includes for reporting and downstream handling, rejects CrossGL include cycles, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, verifies include-path forwarding metadata for every registered source frontend, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, target, source backend, and variant, reports resolved include read failures with project.scan.include-read-failed diagnostics while preserving the already discovered include dependency, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including target and variant rollups, missing include-path processing target and variant rollups, and text inspection identifies inactive include directories by path, resolved path, and frontend visibility, include dependency status, kind, source-backend, source-backend status, resolution-source, variant, and include-path processing rollups by status, target, source backend, and variant, resolved include dependency samples, system include dependency samples, and unresolved include dependency samples with source-backend labels, project-define provenance, variant names, unit source hash and byte-size metadata, and resolved include hash and byte-size metadata, sampled include-path processing artifact metadata, and include-path processing inspection summaries include configured include-directory status records plus frontend-visible and inactive directory counts. It applies named-variant define maps and project include directories to DirectX/HLSL and Metal/MSL project translation with resolved local headers and variant-specific conditional output, Slang project translation with resolved local headers and variant-specific conditional output, Vulkan project translation with a resolved angle include and variant-specific conditional output, and to CUDA/HIP project translation with unresolved runtime system includes, resolved local headers, and variant-specific conditional output. Include semantics remain backend-dependent, so this remains partial project support. Resolved include dependency reports and inspection samples include byte-size metadata."
   "DirectX / HLSL", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, selected-variant lists, and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, emits scan-time diagnostics for active #error and #warning directives scoped by project and selected variant conditionals, emits warning diagnostics when active #define or #undef directives in translation units or resolved include files shadow configured project or selected variant define names while ignoring block-commented directives, inspection summaries and text output expose variant names, selected-variant summaries for scoped runs, and per-variant define counts without define values plus define-processing status, target, source-backend, and variant rollups plus sampled define-processing artifact metadata with define names, deterministic define fingerprints, and without define values, validates malformed define/variant metadata including empty mapping keys, punctuation-bearing variant keys in config/report diagnostics, and forged variant define counts, duplicate selected variant metadata, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including target and variant rollups, missing define-processing target and variant rollups, rejects malformed define, variant, and selected-variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, verifies define-forwarding metadata for every registered source frontend, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, applies named-variant define maps through OpenGL/GLSL native preprocessing during project translation, applies named-variant define maps through DirectX/HLSL native preprocessing during project translation, applies named-variant define maps through Metal/MSL native preprocessing during project translation, applies named-variant define maps through CUDA/HIP native preprocessing during project translation, applies named-variant define maps through Slang native preprocessing during project translation, applies named-variant define maps through Vulkan native preprocessing during project translation, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports configured selected_variants defaults plus API, scan/report CLI, and translation CLI scoped runs for selected declared variants, de-duplicates repeated selections before scan or artifact planning, and does not claim omitted variants as attempted, and sanitizes unsafe variant names into stable output path segments while retaining original variant labels in reports and validation rollups. Native preprocessor semantics remain backend-dependent, so this remains partial project support. Project inspection exposes project-level define names and deterministic define fingerprints, variant define names, and deterministic per-variant define fingerprints without define values. Define-processing inspection summaries expose redacted project define names, deterministic define fingerprints, selected variant names, per-variant define records, and artifact effective define fingerprints without define values."
   "OpenGL / GLSL", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, selected-variant lists, and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, emits scan-time diagnostics for active #error and #warning directives scoped by project and selected variant conditionals, emits warning diagnostics when active #define or #undef directives in translation units or resolved include files shadow configured project or selected variant define names while ignoring block-commented directives, inspection summaries and text output expose variant names, selected-variant summaries for scoped runs, and per-variant define counts without define values plus define-processing status, target, source-backend, and variant rollups plus sampled define-processing artifact metadata with define names, deterministic define fingerprints, and without define values, validates malformed define/variant metadata including empty mapping keys, punctuation-bearing variant keys in config/report diagnostics, and forged variant define counts, duplicate selected variant metadata, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including target and variant rollups, missing define-processing target and variant rollups, rejects malformed define, variant, and selected-variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, verifies define-forwarding metadata for every registered source frontend, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, applies named-variant define maps through OpenGL/GLSL native preprocessing during project translation, applies named-variant define maps through DirectX/HLSL native preprocessing during project translation, applies named-variant define maps through Metal/MSL native preprocessing during project translation, applies named-variant define maps through CUDA/HIP native preprocessing during project translation, applies named-variant define maps through Slang native preprocessing during project translation, applies named-variant define maps through Vulkan native preprocessing during project translation, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports configured selected_variants defaults plus API, scan/report CLI, and translation CLI scoped runs for selected declared variants, de-duplicates repeated selections before scan or artifact planning, and does not claim omitted variants as attempted, and sanitizes unsafe variant names into stable output path segments while retaining original variant labels in reports and validation rollups. Native preprocessor semantics remain backend-dependent, so this remains partial project support. Project inspection exposes project-level define names and deterministic define fingerprints, variant define names, and deterministic per-variant define fingerprints without define values. Define-processing inspection summaries expose redacted project define names, deterministic define fingerprints, selected variant names, per-variant define records, and artifact effective define fingerprints without define values."
   "Metal", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, selected-variant lists, and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, emits scan-time diagnostics for active #error and #warning directives scoped by project and selected variant conditionals, emits warning diagnostics when active #define or #undef directives in translation units or resolved include files shadow configured project or selected variant define names while ignoring block-commented directives, inspection summaries and text output expose variant names, selected-variant summaries for scoped runs, and per-variant define counts without define values plus define-processing status, target, source-backend, and variant rollups plus sampled define-processing artifact metadata with define names, deterministic define fingerprints, and without define values, validates malformed define/variant metadata including empty mapping keys, punctuation-bearing variant keys in config/report diagnostics, and forged variant define counts, duplicate selected variant metadata, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including target and variant rollups, missing define-processing target and variant rollups, rejects malformed define, variant, and selected-variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, verifies define-forwarding metadata for every registered source frontend, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, applies named-variant define maps through OpenGL/GLSL native preprocessing during project translation, applies named-variant define maps through DirectX/HLSL native preprocessing during project translation, applies named-variant define maps through Metal/MSL native preprocessing during project translation, applies named-variant define maps through CUDA/HIP native preprocessing during project translation, applies named-variant define maps through Slang native preprocessing during project translation, applies named-variant define maps through Vulkan native preprocessing during project translation, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports configured selected_variants defaults plus API, scan/report CLI, and translation CLI scoped runs for selected declared variants, de-duplicates repeated selections before scan or artifact planning, and does not claim omitted variants as attempted, and sanitizes unsafe variant names into stable output path segments while retaining original variant labels in reports and validation rollups. Native preprocessor semantics remain backend-dependent, so this remains partial project support. Project inspection exposes project-level define names and deterministic define fingerprints, variant define names, and deterministic per-variant define fingerprints without define values. Define-processing inspection summaries expose redacted project define names, deterministic define fingerprints, selected variant names, per-variant define records, and artifact effective define fingerprints without define values."
   "DirectX / HLSL", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes and byte sizes, artifact source hashes and byte sizes, generated artifact hashes and byte sizes, relative paths, source backend names, intermediate CrossGL provenance, file-span artifact source-map anchors, line-preserving source-map mappings, and line-preserving source-map validation with expected/actual line-preserving mapping mismatch context, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, including fine-grained totals derived from non-file map granularity, source-remap summary totals for artifact count and mapping count plus rollups by granularity, target, source backend, and variant, sanitized variant output segments in source-map and source-remap paths while retaining original variant labels in reports, stable repository-relative POSIX report identity paths for source provenance and source-remap metadata, canonical source-map and source-remap target metadata, source-map, source-remap target, source-remap mapping-count, and source-remap sidecar hash and byte-size metadata inspection, bounded source-map and source-remap artifact inspection samples with source/generated span, mapping-count, source and generated hash and byte-size metadata, sidecar hash plus byte-size metadata, failed validation status metadata, and custom limits, and text inspection derives file-level source-map counts from total and fine-grained source-map totals and reports aggregate source-remap mapping counts, artifact provenance summary rollups by pipeline, intermediate marker, source backend with intermediate marker, target with intermediate marker, and variant with intermediate marker, bounded direct, bridged, and combined artifact provenance inspection samples with source hash and byte-size metadata plus generated hash and byte-size metadata, failed validation status metadata, and custom limits, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and source-map-derived generated/original mappings for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, source-size, generated hash, generated-size, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts including source-size and generated-size counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance with expected/actual source override provenance mismatch context, unit source hash records that are missing, malformed, or no longer match the current source file, unit source byte-size records that are missing, malformed, or no longer match the current source file, with expected/actual unit source hash and byte-size mismatch context, artifact source byte-size records that are missing, malformed, or inconsistent with declared units, with expected/actual artifact source hash and byte-size mismatch context, source-size validation records that no longer match the current source artifact, artifact generated byte-size records that are missing or malformed, generated-size validation records that no longer match the current generated artifact, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, with expected/actual artifact identity mismatch context, source-map records, closed source-map field sets, non-empty source-map mappings, file-level source-map mapping cardinality, fine-grained positive-length source-map mappings, fine-grained source-map byte and line/column span containment, source-map span consistency, source-map anchor consistency, expected/actual source-map and source-remap mismatch context, current artifact-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, non-CrossGL target artifacts with source-remap metadata, source-remap summary rollups by granularity and including variant rollups, missing source-map and source-remap granularity or variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, source-remap mapping-count summary mismatches, source-remap mapping-count mismatches, source-remap sidecar size mismatches, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. SARIF remapped diagnostics with sanitized diagnosticLocation and originalLocation result properties. Generated line source-map mappings are available for line-preserving artifacts; backend-generated line, statement, and token source-map mappings remain unavailable in the translator project pipeline."
   "OpenGL / GLSL", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes and byte sizes, artifact source hashes and byte sizes, generated artifact hashes and byte sizes, relative paths, source backend names, intermediate CrossGL provenance, file-span artifact source-map anchors, line-preserving source-map mappings, and line-preserving source-map validation with expected/actual line-preserving mapping mismatch context, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, including fine-grained totals derived from non-file map granularity, source-remap summary totals for artifact count and mapping count plus rollups by granularity, target, source backend, and variant, sanitized variant output segments in source-map and source-remap paths while retaining original variant labels in reports, stable repository-relative POSIX report identity paths for source provenance and source-remap metadata, canonical source-map and source-remap target metadata, source-map, source-remap target, source-remap mapping-count, and source-remap sidecar hash and byte-size metadata inspection, bounded source-map and source-remap artifact inspection samples with source/generated span, mapping-count, source and generated hash and byte-size metadata, sidecar hash plus byte-size metadata, failed validation status metadata, and custom limits, and text inspection derives file-level source-map counts from total and fine-grained source-map totals and reports aggregate source-remap mapping counts, artifact provenance summary rollups by pipeline, intermediate marker, source backend with intermediate marker, target with intermediate marker, and variant with intermediate marker, bounded direct, bridged, and combined artifact provenance inspection samples with source hash and byte-size metadata plus generated hash and byte-size metadata, failed validation status metadata, and custom limits, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and source-map-derived generated/original mappings for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, source-size, generated hash, generated-size, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts including source-size and generated-size counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance with expected/actual source override provenance mismatch context, unit source hash records that are missing, malformed, or no longer match the current source file, unit source byte-size records that are missing, malformed, or no longer match the current source file, with expected/actual unit source hash and byte-size mismatch context, artifact source byte-size records that are missing, malformed, or inconsistent with declared units, with expected/actual artifact source hash and byte-size mismatch context, source-size validation records that no longer match the current source artifact, artifact generated byte-size records that are missing or malformed, generated-size validation records that no longer match the current generated artifact, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, with expected/actual artifact identity mismatch context, source-map records, closed source-map field sets, non-empty source-map mappings, file-level source-map mapping cardinality, fine-grained positive-length source-map mappings, fine-grained source-map byte and line/column span containment, source-map span consistency, source-map anchor consistency, expected/actual source-map and source-remap mismatch context, current artifact-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, non-CrossGL target artifacts with source-remap metadata, source-remap summary rollups by granularity and including variant rollups, missing source-map and source-remap granularity or variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, source-remap mapping-count summary mismatches, source-remap mapping-count mismatches, source-remap sidecar size mismatches, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. SARIF remapped diagnostics with sanitized diagnosticLocation and originalLocation result properties. Generated line source-map mappings are available for line-preserving artifacts; backend-generated line, statement, and token source-map mappings remain unavailable in the translator project pipeline."
   "Metal", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes and byte sizes, artifact source hashes and byte sizes, generated artifact hashes and byte sizes, relative paths, source backend names, intermediate CrossGL provenance, file-span artifact source-map anchors, line-preserving source-map mappings, and line-preserving source-map validation with expected/actual line-preserving mapping mismatch context, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, including fine-grained totals derived from non-file map granularity, source-remap summary totals for artifact count and mapping count plus rollups by granularity, target, source backend, and variant, sanitized variant output segments in source-map and source-remap paths while retaining original variant labels in reports, stable repository-relative POSIX report identity paths for source provenance and source-remap metadata, canonical source-map and source-remap target metadata, source-map, source-remap target, source-remap mapping-count, and source-remap sidecar hash and byte-size metadata inspection, bounded source-map and source-remap artifact inspection samples with source/generated span, mapping-count, source and generated hash and byte-size metadata, sidecar hash plus byte-size metadata, failed validation status metadata, and custom limits, and text inspection derives file-level source-map counts from total and fine-grained source-map totals and reports aggregate source-remap mapping counts, artifact provenance summary rollups by pipeline, intermediate marker, source backend with intermediate marker, target with intermediate marker, and variant with intermediate marker, bounded direct, bridged, and combined artifact provenance inspection samples with source hash and byte-size metadata plus generated hash and byte-size metadata, failed validation status metadata, and custom limits, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and source-map-derived generated/original mappings for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, source-size, generated hash, generated-size, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts including source-size and generated-size counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance with expected/actual source override provenance mismatch context, unit source hash records that are missing, malformed, or no longer match the current source file, unit source byte-size records that are missing, malformed, or no longer match the current source file, with expected/actual unit source hash and byte-size mismatch context, artifact source byte-size records that are missing, malformed, or inconsistent with declared units, with expected/actual artifact source hash and byte-size mismatch context, source-size validation records that no longer match the current source artifact, artifact generated byte-size records that are missing or malformed, generated-size validation records that no longer match the current generated artifact, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, with expected/actual artifact identity mismatch context, source-map records, closed source-map field sets, non-empty source-map mappings, file-level source-map mapping cardinality, fine-grained positive-length source-map mappings, fine-grained source-map byte and line/column span containment, source-map span consistency, source-map anchor consistency, expected/actual source-map and source-remap mismatch context, current artifact-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, non-CrossGL target artifacts with source-remap metadata, source-remap summary rollups by granularity and including variant rollups, missing source-map and source-remap granularity or variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, source-remap mapping-count summary mismatches, source-remap mapping-count mismatches, source-remap sidecar size mismatches, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. SARIF remapped diagnostics with sanitized diagnosticLocation and originalLocation result properties. Generated line source-map mappings are available for line-preserving artifacts; backend-generated line, statement, and token source-map mappings remain unavailable in the translator project pipeline."

Project Porting Focus
---------------------

This view isolates repository-scale scanning, translation, report
inspection, diagnostics, validation, and corpus-coverage rows.

.. csv-table:: Project-porting status summary
   :header: "Backend", "supported", "partial", "diagnostic", "validated_rejection", "unsupported", "unknown"

   "DirectX / HLSL", "7", "3", "1", "0", "0", "0"
   "OpenGL / GLSL", "7", "3", "1", "0", "0", "0"
   "Metal", "7", "3", "1", "0", "0", "0"
   "Vulkan SPIR-V", "7", "3", "1", "0", "0", "0"
   "CUDA", "7", "3", "1", "0", "0", "0"
   "HIP", "7", "3", "1", "0", "0", "0"
   "Mojo", "7", "3", "1", "0", "0", "0"
   "Rust", "7", "3", "1", "0", "0", "0"
   "Slang", "7", "3", "1", "0", "0", "0"

.. csv-table:: Project-porting actionable backlog
   :header: "Backend", "Feature", "Status", "Current gap", "Next scope"

   "DirectX / HLSL", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend."
   "OpenGL / GLSL", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend."
   "Metal", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend."
   "Vulkan SPIR-V", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend."
   "CUDA", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend."
   "HIP", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend."
   "Mojo", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend."
   "Rust", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend."
   "Slang", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend."
   "DirectX / HLSL", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend."
   "OpenGL / GLSL", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend."
   "Metal", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend."
   "Vulkan SPIR-V", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend."
   "CUDA", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend."
   "HIP", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend."
   "Mojo", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend."
   "Rust", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend."
   "Slang", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend."
   "DirectX / HLSL", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data."
   "OpenGL / GLSL", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data."
   "Metal", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data."
   "Vulkan SPIR-V", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data."
   "CUDA", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data."
   "HIP", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data."
   "Mojo", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data."
   "Rust", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data."
   "Slang", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data."

Feature Matrix
--------------

Each category below uses the status codes from the legend.

.. csv-table:: target
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "CrossGL to target code generation", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: source
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Native source to CrossGL", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Native lexer coverage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Native preprocessor handling", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: stages
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Vertex stage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Fragment/pixel stage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Compute stage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Geometry stage", "Y", "Y", "D", "Y", "D", "D", "D", "D", "Y"
   "Tessellation stages", "Y", "D", "D", "Y", "D", "D", "D", "D", "Y"
   "Mesh/task/amplification stages", "Y", "Y", "Y", "Y", "D", "D", "D", "D", "D"
   "Ray tracing stages", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: stage I/O
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Stage parameter semantics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Direct function return semantics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Struct member semantics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: resources
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Explicit and automatic resource bindings", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Constant/uniform buffers", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Structured/storage buffers", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Resource arrays", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Texture and sampler object model", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "GLSL buffer block lowering", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Resource memory qualifiers", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: textures
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Texture sampling", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Texture LOD, gradient, and offset operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Projected texture operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Texture gather operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Shadow compare texture operations", "Y", "Y", "Y", "Y", "D", "D", "Y", "Y", "Y"
   "Texture query operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Texel fetch operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Multisample texture operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Advanced texture operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: images
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Storage image load/store", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Image atomics", "Y", "Y", "Y", "Y", "D", "D", "Y", "Y", "Y"
   "Multisample storage images", "D", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: language
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Struct declarations and construction", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Array declarations and access", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Function declarations and calls", "Y", "Y", "Y", "D", "D", "D", "D", "Y", "D"
   "Control flow", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Synchronization and memory barriers", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Wave/subgroup intrinsics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Match/pattern lowering", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Vector and matrix expressions", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Bitwise operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: validation
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Invalid shader shape validation", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: project
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Repository source discovery", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Project include resolution", "P", "P", "P", "P", "P", "P", "P", "P", "P"
   "Macro and define variants", "P", "P", "P", "P", "P", "P", "P", "P", "P"
   "Batch project translation", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Optional validation hooks", "D", "D", "D", "D", "D", "D", "D", "D", "D"
   "Migration action report", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Project report inspection", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Artifact manifest", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Source provenance and source-map bridge", "P", "P", "P", "P", "P", "P", "P", "P", "P"
   "Project diagnostics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "External corpus coverage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

Actionable Backlog
------------------

These rows are actionable coverage gaps with ``partial``,
``unsupported``, or ``unknown`` status. Evidence-backed
``diagnostic`` and ``validated_rejection`` rows remain visible in
the matrix counts, but are closed-loop behavior rather than synced
implementation backlog work. Unknown rows need an audit before
implementation work can be scoped accurately.

.. csv-table:: Actionable backlog rows
   :header: "Backend", "Category", "Feature", "Status", "Current gap", "Next scope", "Notes"

   "DirectX / HLSL", "project", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend.", "The project pipeline records include directories, records per-include directory status records and status counts, validates current include-directory resolved paths with expected/actual include-directory status, resolved-path, and frontend-visibility validation context, rejects unexpected generated include-directory status fields, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, recursively scans #include directives into per-unit includeDependencies records, ignores block-commented preprocessor directives while still recognizing directives after same-line block comments with optional nested source paths while honoring simple #if, #ifdef, #ifndef, #elif, #else, and #endif branches with project and variant defines, including defined checks, boolean operators, parentheses, integer and boolean define values, and simple integer comparisons, local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes and byte sizes, source/include-dir resolution source, and project-define plus variant provenance for define-backed include targets, emits structured diagnostics for missing local, dynamic, outside-project, and cyclic include directives with project-define and variant provenance, including structured variant fields and diagnostic variant rollups for missing, dynamic, and cyclic include diagnostics when applicable, validates include dependency record shape including unexpected generated include dependency fields, summary rollups including source-backend, source-backend status, resolution-source, and variant counts, missing current include-scan diagnostics, missing include dependency summary rollups, missing and extra current-scan dependency records, current status, resolved path, resolved include hash, resolved include size, expected/actual include status, path, source, hash, and byte-size mismatch context, source/include-dir resolution source, project-define plus variant include provenance, and current-scan mismatch labels include resolved path, resolution source, and project-define provenance, applies CLI include-directory overrides with normalized repository-relative separators and rejects empty CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, resolves CrossGL quoted includes relative to the current source file and configured include paths, applies named-variant define maps and project include directories to OpenGL/GLSL translation with a resolved angle include and variant-specific conditional output, preserves unresolved CrossGL includes for reporting and downstream handling, rejects CrossGL include cycles, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, verifies include-path forwarding metadata for every registered source frontend, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, target, source backend, and variant, reports resolved include read failures with project.scan.include-read-failed diagnostics while preserving the already discovered include dependency, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including target and variant rollups, missing include-path processing target and variant rollups, and text inspection identifies inactive include directories by path, resolved path, and frontend visibility, include dependency status, kind, source-backend, source-backend status, resolution-source, variant, and include-path processing rollups by status, target, source backend, and variant, resolved include dependency samples, system include dependency samples, and unresolved include dependency samples with source-backend labels, project-define provenance, variant names, unit source hash and byte-size metadata, and resolved include hash and byte-size metadata, sampled include-path processing artifact metadata, and include-path processing inspection summaries include configured include-directory status records plus frontend-visible and inactive directory counts. It applies named-variant define maps and project include directories to DirectX/HLSL and Metal/MSL project translation with resolved local headers and variant-specific conditional output, Slang project translation with resolved local headers and variant-specific conditional output, Vulkan project translation with a resolved angle include and variant-specific conditional output, and to CUDA/HIP project translation with unresolved runtime system includes, resolved local headers, and variant-specific conditional output. Include semantics remain backend-dependent, so this remains partial project support. Resolved include dependency reports and inspection samples include byte-size metadata."
   "OpenGL / GLSL", "project", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend.", "The project pipeline records include directories, records per-include directory status records and status counts, validates current include-directory resolved paths with expected/actual include-directory status, resolved-path, and frontend-visibility validation context, rejects unexpected generated include-directory status fields, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, recursively scans #include directives into per-unit includeDependencies records, ignores block-commented preprocessor directives while still recognizing directives after same-line block comments with optional nested source paths while honoring simple #if, #ifdef, #ifndef, #elif, #else, and #endif branches with project and variant defines, including defined checks, boolean operators, parentheses, integer and boolean define values, and simple integer comparisons, local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes and byte sizes, source/include-dir resolution source, and project-define plus variant provenance for define-backed include targets, emits structured diagnostics for missing local, dynamic, outside-project, and cyclic include directives with project-define and variant provenance, including structured variant fields and diagnostic variant rollups for missing, dynamic, and cyclic include diagnostics when applicable, validates include dependency record shape including unexpected generated include dependency fields, summary rollups including source-backend, source-backend status, resolution-source, and variant counts, missing current include-scan diagnostics, missing include dependency summary rollups, missing and extra current-scan dependency records, current status, resolved path, resolved include hash, resolved include size, expected/actual include status, path, source, hash, and byte-size mismatch context, source/include-dir resolution source, project-define plus variant include provenance, and current-scan mismatch labels include resolved path, resolution source, and project-define provenance, applies CLI include-directory overrides with normalized repository-relative separators and rejects empty CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, resolves CrossGL quoted includes relative to the current source file and configured include paths, applies named-variant define maps and project include directories to OpenGL/GLSL translation with a resolved angle include and variant-specific conditional output, preserves unresolved CrossGL includes for reporting and downstream handling, rejects CrossGL include cycles, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, verifies include-path forwarding metadata for every registered source frontend, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, target, source backend, and variant, reports resolved include read failures with project.scan.include-read-failed diagnostics while preserving the already discovered include dependency, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including target and variant rollups, missing include-path processing target and variant rollups, and text inspection identifies inactive include directories by path, resolved path, and frontend visibility, include dependency status, kind, source-backend, source-backend status, resolution-source, variant, and include-path processing rollups by status, target, source backend, and variant, resolved include dependency samples, system include dependency samples, and unresolved include dependency samples with source-backend labels, project-define provenance, variant names, unit source hash and byte-size metadata, and resolved include hash and byte-size metadata, sampled include-path processing artifact metadata, and include-path processing inspection summaries include configured include-directory status records plus frontend-visible and inactive directory counts. It applies named-variant define maps and project include directories to DirectX/HLSL and Metal/MSL project translation with resolved local headers and variant-specific conditional output, Slang project translation with resolved local headers and variant-specific conditional output, Vulkan project translation with a resolved angle include and variant-specific conditional output, and to CUDA/HIP project translation with unresolved runtime system includes, resolved local headers, and variant-specific conditional output. Include semantics remain backend-dependent, so this remains partial project support. Resolved include dependency reports and inspection samples include byte-size metadata."
   "Metal", "project", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend.", "The project pipeline records include directories, records per-include directory status records and status counts, validates current include-directory resolved paths with expected/actual include-directory status, resolved-path, and frontend-visibility validation context, rejects unexpected generated include-directory status fields, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, recursively scans #include directives into per-unit includeDependencies records, ignores block-commented preprocessor directives while still recognizing directives after same-line block comments with optional nested source paths while honoring simple #if, #ifdef, #ifndef, #elif, #else, and #endif branches with project and variant defines, including defined checks, boolean operators, parentheses, integer and boolean define values, and simple integer comparisons, local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes and byte sizes, source/include-dir resolution source, and project-define plus variant provenance for define-backed include targets, emits structured diagnostics for missing local, dynamic, outside-project, and cyclic include directives with project-define and variant provenance, including structured variant fields and diagnostic variant rollups for missing, dynamic, and cyclic include diagnostics when applicable, validates include dependency record shape including unexpected generated include dependency fields, summary rollups including source-backend, source-backend status, resolution-source, and variant counts, missing current include-scan diagnostics, missing include dependency summary rollups, missing and extra current-scan dependency records, current status, resolved path, resolved include hash, resolved include size, expected/actual include status, path, source, hash, and byte-size mismatch context, source/include-dir resolution source, project-define plus variant include provenance, and current-scan mismatch labels include resolved path, resolution source, and project-define provenance, applies CLI include-directory overrides with normalized repository-relative separators and rejects empty CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, resolves CrossGL quoted includes relative to the current source file and configured include paths, applies named-variant define maps and project include directories to OpenGL/GLSL translation with a resolved angle include and variant-specific conditional output, preserves unresolved CrossGL includes for reporting and downstream handling, rejects CrossGL include cycles, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, verifies include-path forwarding metadata for every registered source frontend, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, target, source backend, and variant, reports resolved include read failures with project.scan.include-read-failed diagnostics while preserving the already discovered include dependency, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including target and variant rollups, missing include-path processing target and variant rollups, and text inspection identifies inactive include directories by path, resolved path, and frontend visibility, include dependency status, kind, source-backend, source-backend status, resolution-source, variant, and include-path processing rollups by status, target, source backend, and variant, resolved include dependency samples, system include dependency samples, and unresolved include dependency samples with source-backend labels, project-define provenance, variant names, unit source hash and byte-size metadata, and resolved include hash and byte-size metadata, sampled include-path processing artifact metadata, and include-path processing inspection summaries include configured include-directory status records plus frontend-visible and inactive directory counts. It applies named-variant define maps and project include directories to DirectX/HLSL and Metal/MSL project translation with resolved local headers and variant-specific conditional output, Slang project translation with resolved local headers and variant-specific conditional output, Vulkan project translation with a resolved angle include and variant-specific conditional output, and to CUDA/HIP project translation with unresolved runtime system includes, resolved local headers, and variant-specific conditional output. Include semantics remain backend-dependent, so this remains partial project support. Resolved include dependency reports and inspection samples include byte-size metadata."
   "Vulkan SPIR-V", "project", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend.", "The project pipeline records include directories, records per-include directory status records and status counts, validates current include-directory resolved paths with expected/actual include-directory status, resolved-path, and frontend-visibility validation context, rejects unexpected generated include-directory status fields, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, recursively scans #include directives into per-unit includeDependencies records, ignores block-commented preprocessor directives while still recognizing directives after same-line block comments with optional nested source paths while honoring simple #if, #ifdef, #ifndef, #elif, #else, and #endif branches with project and variant defines, including defined checks, boolean operators, parentheses, integer and boolean define values, and simple integer comparisons, local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes and byte sizes, source/include-dir resolution source, and project-define plus variant provenance for define-backed include targets, emits structured diagnostics for missing local, dynamic, outside-project, and cyclic include directives with project-define and variant provenance, including structured variant fields and diagnostic variant rollups for missing, dynamic, and cyclic include diagnostics when applicable, validates include dependency record shape including unexpected generated include dependency fields, summary rollups including source-backend, source-backend status, resolution-source, and variant counts, missing current include-scan diagnostics, missing include dependency summary rollups, missing and extra current-scan dependency records, current status, resolved path, resolved include hash, resolved include size, expected/actual include status, path, source, hash, and byte-size mismatch context, source/include-dir resolution source, project-define plus variant include provenance, and current-scan mismatch labels include resolved path, resolution source, and project-define provenance, applies CLI include-directory overrides with normalized repository-relative separators and rejects empty CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, resolves CrossGL quoted includes relative to the current source file and configured include paths, applies named-variant define maps and project include directories to OpenGL/GLSL translation with a resolved angle include and variant-specific conditional output, preserves unresolved CrossGL includes for reporting and downstream handling, rejects CrossGL include cycles, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, verifies include-path forwarding metadata for every registered source frontend, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, target, source backend, and variant, reports resolved include read failures with project.scan.include-read-failed diagnostics while preserving the already discovered include dependency, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including target and variant rollups, missing include-path processing target and variant rollups, and text inspection identifies inactive include directories by path, resolved path, and frontend visibility, include dependency status, kind, source-backend, source-backend status, resolution-source, variant, and include-path processing rollups by status, target, source backend, and variant, resolved include dependency samples, system include dependency samples, and unresolved include dependency samples with source-backend labels, project-define provenance, variant names, unit source hash and byte-size metadata, and resolved include hash and byte-size metadata, sampled include-path processing artifact metadata, and include-path processing inspection summaries include configured include-directory status records plus frontend-visible and inactive directory counts. It applies named-variant define maps and project include directories to DirectX/HLSL and Metal/MSL project translation with resolved local headers and variant-specific conditional output, Slang project translation with resolved local headers and variant-specific conditional output, Vulkan project translation with a resolved angle include and variant-specific conditional output, and to CUDA/HIP project translation with unresolved runtime system includes, resolved local headers, and variant-specific conditional output. Include semantics remain backend-dependent, so this remains partial project support. Resolved include dependency reports and inspection samples include byte-size metadata."
   "CUDA", "project", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend.", "The project pipeline records include directories, records per-include directory status records and status counts, validates current include-directory resolved paths with expected/actual include-directory status, resolved-path, and frontend-visibility validation context, rejects unexpected generated include-directory status fields, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, recursively scans #include directives into per-unit includeDependencies records, ignores block-commented preprocessor directives while still recognizing directives after same-line block comments with optional nested source paths while honoring simple #if, #ifdef, #ifndef, #elif, #else, and #endif branches with project and variant defines, including defined checks, boolean operators, parentheses, integer and boolean define values, and simple integer comparisons, local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes and byte sizes, source/include-dir resolution source, and project-define plus variant provenance for define-backed include targets, emits structured diagnostics for missing local, dynamic, outside-project, and cyclic include directives with project-define and variant provenance, including structured variant fields and diagnostic variant rollups for missing, dynamic, and cyclic include diagnostics when applicable, validates include dependency record shape including unexpected generated include dependency fields, summary rollups including source-backend, source-backend status, resolution-source, and variant counts, missing current include-scan diagnostics, missing include dependency summary rollups, missing and extra current-scan dependency records, current status, resolved path, resolved include hash, resolved include size, expected/actual include status, path, source, hash, and byte-size mismatch context, source/include-dir resolution source, project-define plus variant include provenance, and current-scan mismatch labels include resolved path, resolution source, and project-define provenance, applies CLI include-directory overrides with normalized repository-relative separators and rejects empty CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, resolves CrossGL quoted includes relative to the current source file and configured include paths, applies named-variant define maps and project include directories to OpenGL/GLSL translation with a resolved angle include and variant-specific conditional output, preserves unresolved CrossGL includes for reporting and downstream handling, rejects CrossGL include cycles, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, verifies include-path forwarding metadata for every registered source frontend, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, target, source backend, and variant, reports resolved include read failures with project.scan.include-read-failed diagnostics while preserving the already discovered include dependency, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including target and variant rollups, missing include-path processing target and variant rollups, and text inspection identifies inactive include directories by path, resolved path, and frontend visibility, include dependency status, kind, source-backend, source-backend status, resolution-source, variant, and include-path processing rollups by status, target, source backend, and variant, resolved include dependency samples, system include dependency samples, and unresolved include dependency samples with source-backend labels, project-define provenance, variant names, unit source hash and byte-size metadata, and resolved include hash and byte-size metadata, sampled include-path processing artifact metadata, and include-path processing inspection summaries include configured include-directory status records plus frontend-visible and inactive directory counts. It applies named-variant define maps and project include directories to DirectX/HLSL and Metal/MSL project translation with resolved local headers and variant-specific conditional output, Slang project translation with resolved local headers and variant-specific conditional output, Vulkan project translation with a resolved angle include and variant-specific conditional output, and to CUDA/HIP project translation with unresolved runtime system includes, resolved local headers, and variant-specific conditional output. Include semantics remain backend-dependent, so this remains partial project support. Resolved include dependency reports and inspection samples include byte-size metadata."
   "HIP", "project", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend.", "The project pipeline records include directories, records per-include directory status records and status counts, validates current include-directory resolved paths with expected/actual include-directory status, resolved-path, and frontend-visibility validation context, rejects unexpected generated include-directory status fields, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, recursively scans #include directives into per-unit includeDependencies records, ignores block-commented preprocessor directives while still recognizing directives after same-line block comments with optional nested source paths while honoring simple #if, #ifdef, #ifndef, #elif, #else, and #endif branches with project and variant defines, including defined checks, boolean operators, parentheses, integer and boolean define values, and simple integer comparisons, local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes and byte sizes, source/include-dir resolution source, and project-define plus variant provenance for define-backed include targets, emits structured diagnostics for missing local, dynamic, outside-project, and cyclic include directives with project-define and variant provenance, including structured variant fields and diagnostic variant rollups for missing, dynamic, and cyclic include diagnostics when applicable, validates include dependency record shape including unexpected generated include dependency fields, summary rollups including source-backend, source-backend status, resolution-source, and variant counts, missing current include-scan diagnostics, missing include dependency summary rollups, missing and extra current-scan dependency records, current status, resolved path, resolved include hash, resolved include size, expected/actual include status, path, source, hash, and byte-size mismatch context, source/include-dir resolution source, project-define plus variant include provenance, and current-scan mismatch labels include resolved path, resolution source, and project-define provenance, applies CLI include-directory overrides with normalized repository-relative separators and rejects empty CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, resolves CrossGL quoted includes relative to the current source file and configured include paths, applies named-variant define maps and project include directories to OpenGL/GLSL translation with a resolved angle include and variant-specific conditional output, preserves unresolved CrossGL includes for reporting and downstream handling, rejects CrossGL include cycles, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, verifies include-path forwarding metadata for every registered source frontend, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, target, source backend, and variant, reports resolved include read failures with project.scan.include-read-failed diagnostics while preserving the already discovered include dependency, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including target and variant rollups, missing include-path processing target and variant rollups, and text inspection identifies inactive include directories by path, resolved path, and frontend visibility, include dependency status, kind, source-backend, source-backend status, resolution-source, variant, and include-path processing rollups by status, target, source backend, and variant, resolved include dependency samples, system include dependency samples, and unresolved include dependency samples with source-backend labels, project-define provenance, variant names, unit source hash and byte-size metadata, and resolved include hash and byte-size metadata, sampled include-path processing artifact metadata, and include-path processing inspection summaries include configured include-directory status records plus frontend-visible and inactive directory counts. It applies named-variant define maps and project include directories to DirectX/HLSL and Metal/MSL project translation with resolved local headers and variant-specific conditional output, Slang project translation with resolved local headers and variant-specific conditional output, Vulkan project translation with a resolved angle include and variant-specific conditional output, and to CUDA/HIP project translation with unresolved runtime system includes, resolved local headers, and variant-specific conditional output. Include semantics remain backend-dependent, so this remains partial project support. Resolved include dependency reports and inspection samples include byte-size metadata."
   "Mojo", "project", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend.", "The project pipeline records include directories, records per-include directory status records and status counts, validates current include-directory resolved paths with expected/actual include-directory status, resolved-path, and frontend-visibility validation context, rejects unexpected generated include-directory status fields, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, recursively scans #include directives into per-unit includeDependencies records, ignores block-commented preprocessor directives while still recognizing directives after same-line block comments with optional nested source paths while honoring simple #if, #ifdef, #ifndef, #elif, #else, and #endif branches with project and variant defines, including defined checks, boolean operators, parentheses, integer and boolean define values, and simple integer comparisons, local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes and byte sizes, source/include-dir resolution source, and project-define plus variant provenance for define-backed include targets, emits structured diagnostics for missing local, dynamic, outside-project, and cyclic include directives with project-define and variant provenance, including structured variant fields and diagnostic variant rollups for missing, dynamic, and cyclic include diagnostics when applicable, validates include dependency record shape including unexpected generated include dependency fields, summary rollups including source-backend, source-backend status, resolution-source, and variant counts, missing current include-scan diagnostics, missing include dependency summary rollups, missing and extra current-scan dependency records, current status, resolved path, resolved include hash, resolved include size, expected/actual include status, path, source, hash, and byte-size mismatch context, source/include-dir resolution source, project-define plus variant include provenance, and current-scan mismatch labels include resolved path, resolution source, and project-define provenance, applies CLI include-directory overrides with normalized repository-relative separators and rejects empty CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, resolves CrossGL quoted includes relative to the current source file and configured include paths, applies named-variant define maps and project include directories to OpenGL/GLSL translation with a resolved angle include and variant-specific conditional output, preserves unresolved CrossGL includes for reporting and downstream handling, rejects CrossGL include cycles, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, verifies include-path forwarding metadata for every registered source frontend, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, target, source backend, and variant, reports resolved include read failures with project.scan.include-read-failed diagnostics while preserving the already discovered include dependency, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including target and variant rollups, missing include-path processing target and variant rollups, and text inspection identifies inactive include directories by path, resolved path, and frontend visibility, include dependency status, kind, source-backend, source-backend status, resolution-source, variant, and include-path processing rollups by status, target, source backend, and variant, resolved include dependency samples, system include dependency samples, and unresolved include dependency samples with source-backend labels, project-define provenance, variant names, unit source hash and byte-size metadata, and resolved include hash and byte-size metadata, sampled include-path processing artifact metadata, and include-path processing inspection summaries include configured include-directory status records plus frontend-visible and inactive directory counts. It applies named-variant define maps and project include directories to DirectX/HLSL and Metal/MSL project translation with resolved local headers and variant-specific conditional output, Slang project translation with resolved local headers and variant-specific conditional output, Vulkan project translation with a resolved angle include and variant-specific conditional output, and to CUDA/HIP project translation with unresolved runtime system includes, resolved local headers, and variant-specific conditional output. Include semantics remain backend-dependent, so this remains partial project support. Resolved include dependency reports and inspection samples include byte-size metadata."
   "Rust", "project", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend.", "The project pipeline records include directories, records per-include directory status records and status counts, validates current include-directory resolved paths with expected/actual include-directory status, resolved-path, and frontend-visibility validation context, rejects unexpected generated include-directory status fields, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, recursively scans #include directives into per-unit includeDependencies records, ignores block-commented preprocessor directives while still recognizing directives after same-line block comments with optional nested source paths while honoring simple #if, #ifdef, #ifndef, #elif, #else, and #endif branches with project and variant defines, including defined checks, boolean operators, parentheses, integer and boolean define values, and simple integer comparisons, local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes and byte sizes, source/include-dir resolution source, and project-define plus variant provenance for define-backed include targets, emits structured diagnostics for missing local, dynamic, outside-project, and cyclic include directives with project-define and variant provenance, including structured variant fields and diagnostic variant rollups for missing, dynamic, and cyclic include diagnostics when applicable, validates include dependency record shape including unexpected generated include dependency fields, summary rollups including source-backend, source-backend status, resolution-source, and variant counts, missing current include-scan diagnostics, missing include dependency summary rollups, missing and extra current-scan dependency records, current status, resolved path, resolved include hash, resolved include size, expected/actual include status, path, source, hash, and byte-size mismatch context, source/include-dir resolution source, project-define plus variant include provenance, and current-scan mismatch labels include resolved path, resolution source, and project-define provenance, applies CLI include-directory overrides with normalized repository-relative separators and rejects empty CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, resolves CrossGL quoted includes relative to the current source file and configured include paths, applies named-variant define maps and project include directories to OpenGL/GLSL translation with a resolved angle include and variant-specific conditional output, preserves unresolved CrossGL includes for reporting and downstream handling, rejects CrossGL include cycles, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, verifies include-path forwarding metadata for every registered source frontend, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, target, source backend, and variant, reports resolved include read failures with project.scan.include-read-failed diagnostics while preserving the already discovered include dependency, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including target and variant rollups, missing include-path processing target and variant rollups, and text inspection identifies inactive include directories by path, resolved path, and frontend visibility, include dependency status, kind, source-backend, source-backend status, resolution-source, variant, and include-path processing rollups by status, target, source backend, and variant, resolved include dependency samples, system include dependency samples, and unresolved include dependency samples with source-backend labels, project-define provenance, variant names, unit source hash and byte-size metadata, and resolved include hash and byte-size metadata, sampled include-path processing artifact metadata, and include-path processing inspection summaries include configured include-directory status records plus frontend-visible and inactive directory counts. It applies named-variant define maps and project include directories to DirectX/HLSL and Metal/MSL project translation with resolved local headers and variant-specific conditional output, Slang project translation with resolved local headers and variant-specific conditional output, Vulkan project translation with a resolved angle include and variant-specific conditional output, and to CUDA/HIP project translation with unresolved runtime system includes, resolved local headers, and variant-specific conditional output. Include semantics remain backend-dependent, so this remains partial project support. Resolved include dependency reports and inspection samples include byte-size metadata."
   "Slang", "project", "Project include resolution", "partial", "Include directories and discovered direct or nested include dependencies are reported, simple conditional include branches are evaluated with project and variant defines, recursive include cycles and resolved include read failures are diagnosed, and include-path forwarding is covered across every registered source frontend as forwarded or explicitly not-supported; system include behavior and backend-native include semantics remain backend-dependent.", "Add backend-native semantic coverage for system includes and translated, preserved, or rejected include forms for each source frontend.", "The project pipeline records include directories, records per-include directory status records and status counts, validates current include-directory resolved paths with expected/actual include-directory status, resolved-path, and frontend-visibility validation context, rejects unexpected generated include-directory status fields, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, recursively scans #include directives into per-unit includeDependencies records, ignores block-commented preprocessor directives while still recognizing directives after same-line block comments with optional nested source paths while honoring simple #if, #ifdef, #ifndef, #elif, #else, and #endif branches with project and variant defines, including defined checks, boolean operators, parentheses, integer and boolean define values, and simple integer comparisons, local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes and byte sizes, source/include-dir resolution source, and project-define plus variant provenance for define-backed include targets, emits structured diagnostics for missing local, dynamic, outside-project, and cyclic include directives with project-define and variant provenance, including structured variant fields and diagnostic variant rollups for missing, dynamic, and cyclic include diagnostics when applicable, validates include dependency record shape including unexpected generated include dependency fields, summary rollups including source-backend, source-backend status, resolution-source, and variant counts, missing current include-scan diagnostics, missing include dependency summary rollups, missing and extra current-scan dependency records, current status, resolved path, resolved include hash, resolved include size, expected/actual include status, path, source, hash, and byte-size mismatch context, source/include-dir resolution source, project-define plus variant include provenance, and current-scan mismatch labels include resolved path, resolution source, and project-define provenance, applies CLI include-directory overrides with normalized repository-relative separators and rejects empty CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, resolves CrossGL quoted includes relative to the current source file and configured include paths, applies named-variant define maps and project include directories to OpenGL/GLSL translation with a resolved angle include and variant-specific conditional output, preserves unresolved CrossGL includes for reporting and downstream handling, rejects CrossGL include cycles, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, verifies include-path forwarding metadata for every registered source frontend, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, target, source backend, and variant, reports resolved include read failures with project.scan.include-read-failed diagnostics while preserving the already discovered include dependency, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including target and variant rollups, missing include-path processing target and variant rollups, and text inspection identifies inactive include directories by path, resolved path, and frontend visibility, include dependency status, kind, source-backend, source-backend status, resolution-source, variant, and include-path processing rollups by status, target, source backend, and variant, resolved include dependency samples, system include dependency samples, and unresolved include dependency samples with source-backend labels, project-define provenance, variant names, unit source hash and byte-size metadata, and resolved include hash and byte-size metadata, sampled include-path processing artifact metadata, and include-path processing inspection summaries include configured include-directory status records plus frontend-visible and inactive directory counts. It applies named-variant define maps and project include directories to DirectX/HLSL and Metal/MSL project translation with resolved local headers and variant-specific conditional output, Slang project translation with resolved local headers and variant-specific conditional output, Vulkan project translation with a resolved angle include and variant-specific conditional output, and to CUDA/HIP project translation with unresolved runtime system includes, resolved local headers, and variant-specific conditional output. Include semantics remain backend-dependent, so this remains partial project support. Resolved include dependency reports and inspection samples include byte-size metadata."
   "DirectX / HLSL", "project", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend.", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, selected-variant lists, and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, emits scan-time diagnostics for active #error and #warning directives scoped by project and selected variant conditionals, emits warning diagnostics when active #define or #undef directives in translation units or resolved include files shadow configured project or selected variant define names while ignoring block-commented directives, inspection summaries and text output expose variant names, selected-variant summaries for scoped runs, and per-variant define counts without define values plus define-processing status, target, source-backend, and variant rollups plus sampled define-processing artifact metadata with define names, deterministic define fingerprints, and without define values, validates malformed define/variant metadata including empty mapping keys, punctuation-bearing variant keys in config/report diagnostics, and forged variant define counts, duplicate selected variant metadata, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including target and variant rollups, missing define-processing target and variant rollups, rejects malformed define, variant, and selected-variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, verifies define-forwarding metadata for every registered source frontend, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, applies named-variant define maps through OpenGL/GLSL native preprocessing during project translation, applies named-variant define maps through DirectX/HLSL native preprocessing during project translation, applies named-variant define maps through Metal/MSL native preprocessing during project translation, applies named-variant define maps through CUDA/HIP native preprocessing during project translation, applies named-variant define maps through Slang native preprocessing during project translation, applies named-variant define maps through Vulkan native preprocessing during project translation, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports configured selected_variants defaults plus API, scan/report CLI, and translation CLI scoped runs for selected declared variants, de-duplicates repeated selections before scan or artifact planning, and does not claim omitted variants as attempted, and sanitizes unsafe variant names into stable output path segments while retaining original variant labels in reports and validation rollups. Native preprocessor semantics remain backend-dependent, so this remains partial project support. Project inspection exposes project-level define names and deterministic define fingerprints, variant define names, and deterministic per-variant define fingerprints without define values. Define-processing inspection summaries expose redacted project define names, deterministic define fingerprints, selected variant names, per-variant define records, and artifact effective define fingerprints without define values."
   "OpenGL / GLSL", "project", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend.", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, selected-variant lists, and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, emits scan-time diagnostics for active #error and #warning directives scoped by project and selected variant conditionals, emits warning diagnostics when active #define or #undef directives in translation units or resolved include files shadow configured project or selected variant define names while ignoring block-commented directives, inspection summaries and text output expose variant names, selected-variant summaries for scoped runs, and per-variant define counts without define values plus define-processing status, target, source-backend, and variant rollups plus sampled define-processing artifact metadata with define names, deterministic define fingerprints, and without define values, validates malformed define/variant metadata including empty mapping keys, punctuation-bearing variant keys in config/report diagnostics, and forged variant define counts, duplicate selected variant metadata, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including target and variant rollups, missing define-processing target and variant rollups, rejects malformed define, variant, and selected-variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, verifies define-forwarding metadata for every registered source frontend, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, applies named-variant define maps through OpenGL/GLSL native preprocessing during project translation, applies named-variant define maps through DirectX/HLSL native preprocessing during project translation, applies named-variant define maps through Metal/MSL native preprocessing during project translation, applies named-variant define maps through CUDA/HIP native preprocessing during project translation, applies named-variant define maps through Slang native preprocessing during project translation, applies named-variant define maps through Vulkan native preprocessing during project translation, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports configured selected_variants defaults plus API, scan/report CLI, and translation CLI scoped runs for selected declared variants, de-duplicates repeated selections before scan or artifact planning, and does not claim omitted variants as attempted, and sanitizes unsafe variant names into stable output path segments while retaining original variant labels in reports and validation rollups. Native preprocessor semantics remain backend-dependent, so this remains partial project support. Project inspection exposes project-level define names and deterministic define fingerprints, variant define names, and deterministic per-variant define fingerprints without define values. Define-processing inspection summaries expose redacted project define names, deterministic define fingerprints, selected variant names, per-variant define records, and artifact effective define fingerprints without define values."
   "Metal", "project", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend.", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, selected-variant lists, and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, emits scan-time diagnostics for active #error and #warning directives scoped by project and selected variant conditionals, emits warning diagnostics when active #define or #undef directives in translation units or resolved include files shadow configured project or selected variant define names while ignoring block-commented directives, inspection summaries and text output expose variant names, selected-variant summaries for scoped runs, and per-variant define counts without define values plus define-processing status, target, source-backend, and variant rollups plus sampled define-processing artifact metadata with define names, deterministic define fingerprints, and without define values, validates malformed define/variant metadata including empty mapping keys, punctuation-bearing variant keys in config/report diagnostics, and forged variant define counts, duplicate selected variant metadata, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including target and variant rollups, missing define-processing target and variant rollups, rejects malformed define, variant, and selected-variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, verifies define-forwarding metadata for every registered source frontend, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, applies named-variant define maps through OpenGL/GLSL native preprocessing during project translation, applies named-variant define maps through DirectX/HLSL native preprocessing during project translation, applies named-variant define maps through Metal/MSL native preprocessing during project translation, applies named-variant define maps through CUDA/HIP native preprocessing during project translation, applies named-variant define maps through Slang native preprocessing during project translation, applies named-variant define maps through Vulkan native preprocessing during project translation, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports configured selected_variants defaults plus API, scan/report CLI, and translation CLI scoped runs for selected declared variants, de-duplicates repeated selections before scan or artifact planning, and does not claim omitted variants as attempted, and sanitizes unsafe variant names into stable output path segments while retaining original variant labels in reports and validation rollups. Native preprocessor semantics remain backend-dependent, so this remains partial project support. Project inspection exposes project-level define names and deterministic define fingerprints, variant define names, and deterministic per-variant define fingerprints without define values. Define-processing inspection summaries expose redacted project define names, deterministic define fingerprints, selected variant names, per-variant define records, and artifact effective define fingerprints without define values."
   "Vulkan SPIR-V", "project", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend.", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, selected-variant lists, and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, emits scan-time diagnostics for active #error and #warning directives scoped by project and selected variant conditionals, emits warning diagnostics when active #define or #undef directives in translation units or resolved include files shadow configured project or selected variant define names while ignoring block-commented directives, inspection summaries and text output expose variant names, selected-variant summaries for scoped runs, and per-variant define counts without define values plus define-processing status, target, source-backend, and variant rollups plus sampled define-processing artifact metadata with define names, deterministic define fingerprints, and without define values, validates malformed define/variant metadata including empty mapping keys, punctuation-bearing variant keys in config/report diagnostics, and forged variant define counts, duplicate selected variant metadata, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including target and variant rollups, missing define-processing target and variant rollups, rejects malformed define, variant, and selected-variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, verifies define-forwarding metadata for every registered source frontend, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, applies named-variant define maps through OpenGL/GLSL native preprocessing during project translation, applies named-variant define maps through DirectX/HLSL native preprocessing during project translation, applies named-variant define maps through Metal/MSL native preprocessing during project translation, applies named-variant define maps through CUDA/HIP native preprocessing during project translation, applies named-variant define maps through Slang native preprocessing during project translation, applies named-variant define maps through Vulkan native preprocessing during project translation, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports configured selected_variants defaults plus API, scan/report CLI, and translation CLI scoped runs for selected declared variants, de-duplicates repeated selections before scan or artifact planning, and does not claim omitted variants as attempted, and sanitizes unsafe variant names into stable output path segments while retaining original variant labels in reports and validation rollups. Native preprocessor semantics remain backend-dependent, so this remains partial project support. Project inspection exposes project-level define names and deterministic define fingerprints, variant define names, and deterministic per-variant define fingerprints without define values. Define-processing inspection summaries expose redacted project define names, deterministic define fingerprints, selected variant names, per-variant define records, and artifact effective define fingerprints without define values."
   "CUDA", "project", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend.", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, selected-variant lists, and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, emits scan-time diagnostics for active #error and #warning directives scoped by project and selected variant conditionals, emits warning diagnostics when active #define or #undef directives in translation units or resolved include files shadow configured project or selected variant define names while ignoring block-commented directives, inspection summaries and text output expose variant names, selected-variant summaries for scoped runs, and per-variant define counts without define values plus define-processing status, target, source-backend, and variant rollups plus sampled define-processing artifact metadata with define names, deterministic define fingerprints, and without define values, validates malformed define/variant metadata including empty mapping keys, punctuation-bearing variant keys in config/report diagnostics, and forged variant define counts, duplicate selected variant metadata, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including target and variant rollups, missing define-processing target and variant rollups, rejects malformed define, variant, and selected-variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, verifies define-forwarding metadata for every registered source frontend, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, applies named-variant define maps through OpenGL/GLSL native preprocessing during project translation, applies named-variant define maps through DirectX/HLSL native preprocessing during project translation, applies named-variant define maps through Metal/MSL native preprocessing during project translation, applies named-variant define maps through CUDA/HIP native preprocessing during project translation, applies named-variant define maps through Slang native preprocessing during project translation, applies named-variant define maps through Vulkan native preprocessing during project translation, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports configured selected_variants defaults plus API, scan/report CLI, and translation CLI scoped runs for selected declared variants, de-duplicates repeated selections before scan or artifact planning, and does not claim omitted variants as attempted, and sanitizes unsafe variant names into stable output path segments while retaining original variant labels in reports and validation rollups. Native preprocessor semantics remain backend-dependent, so this remains partial project support. Project inspection exposes project-level define names and deterministic define fingerprints, variant define names, and deterministic per-variant define fingerprints without define values. Define-processing inspection summaries expose redacted project define names, deterministic define fingerprints, selected variant names, per-variant define records, and artifact effective define fingerprints without define values."
   "HIP", "project", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend.", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, selected-variant lists, and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, emits scan-time diagnostics for active #error and #warning directives scoped by project and selected variant conditionals, emits warning diagnostics when active #define or #undef directives in translation units or resolved include files shadow configured project or selected variant define names while ignoring block-commented directives, inspection summaries and text output expose variant names, selected-variant summaries for scoped runs, and per-variant define counts without define values plus define-processing status, target, source-backend, and variant rollups plus sampled define-processing artifact metadata with define names, deterministic define fingerprints, and without define values, validates malformed define/variant metadata including empty mapping keys, punctuation-bearing variant keys in config/report diagnostics, and forged variant define counts, duplicate selected variant metadata, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including target and variant rollups, missing define-processing target and variant rollups, rejects malformed define, variant, and selected-variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, verifies define-forwarding metadata for every registered source frontend, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, applies named-variant define maps through OpenGL/GLSL native preprocessing during project translation, applies named-variant define maps through DirectX/HLSL native preprocessing during project translation, applies named-variant define maps through Metal/MSL native preprocessing during project translation, applies named-variant define maps through CUDA/HIP native preprocessing during project translation, applies named-variant define maps through Slang native preprocessing during project translation, applies named-variant define maps through Vulkan native preprocessing during project translation, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports configured selected_variants defaults plus API, scan/report CLI, and translation CLI scoped runs for selected declared variants, de-duplicates repeated selections before scan or artifact planning, and does not claim omitted variants as attempted, and sanitizes unsafe variant names into stable output path segments while retaining original variant labels in reports and validation rollups. Native preprocessor semantics remain backend-dependent, so this remains partial project support. Project inspection exposes project-level define names and deterministic define fingerprints, variant define names, and deterministic per-variant define fingerprints without define values. Define-processing inspection summaries expose redacted project define names, deterministic define fingerprints, selected variant names, per-variant define records, and artifact effective define fingerprints without define values."
   "Mojo", "project", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend.", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, selected-variant lists, and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, emits scan-time diagnostics for active #error and #warning directives scoped by project and selected variant conditionals, emits warning diagnostics when active #define or #undef directives in translation units or resolved include files shadow configured project or selected variant define names while ignoring block-commented directives, inspection summaries and text output expose variant names, selected-variant summaries for scoped runs, and per-variant define counts without define values plus define-processing status, target, source-backend, and variant rollups plus sampled define-processing artifact metadata with define names, deterministic define fingerprints, and without define values, validates malformed define/variant metadata including empty mapping keys, punctuation-bearing variant keys in config/report diagnostics, and forged variant define counts, duplicate selected variant metadata, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including target and variant rollups, missing define-processing target and variant rollups, rejects malformed define, variant, and selected-variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, verifies define-forwarding metadata for every registered source frontend, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, applies named-variant define maps through OpenGL/GLSL native preprocessing during project translation, applies named-variant define maps through DirectX/HLSL native preprocessing during project translation, applies named-variant define maps through Metal/MSL native preprocessing during project translation, applies named-variant define maps through CUDA/HIP native preprocessing during project translation, applies named-variant define maps through Slang native preprocessing during project translation, applies named-variant define maps through Vulkan native preprocessing during project translation, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports configured selected_variants defaults plus API, scan/report CLI, and translation CLI scoped runs for selected declared variants, de-duplicates repeated selections before scan or artifact planning, and does not claim omitted variants as attempted, and sanitizes unsafe variant names into stable output path segments while retaining original variant labels in reports and validation rollups. Native preprocessor semantics remain backend-dependent, so this remains partial project support. Project inspection exposes project-level define names and deterministic define fingerprints, variant define names, and deterministic per-variant define fingerprints without define values. Define-processing inspection summaries expose redacted project define names, deterministic define fingerprints, selected variant names, per-variant define records, and artifact effective define fingerprints without define values."
   "Rust", "project", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend.", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, selected-variant lists, and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, emits scan-time diagnostics for active #error and #warning directives scoped by project and selected variant conditionals, emits warning diagnostics when active #define or #undef directives in translation units or resolved include files shadow configured project or selected variant define names while ignoring block-commented directives, inspection summaries and text output expose variant names, selected-variant summaries for scoped runs, and per-variant define counts without define values plus define-processing status, target, source-backend, and variant rollups plus sampled define-processing artifact metadata with define names, deterministic define fingerprints, and without define values, validates malformed define/variant metadata including empty mapping keys, punctuation-bearing variant keys in config/report diagnostics, and forged variant define counts, duplicate selected variant metadata, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including target and variant rollups, missing define-processing target and variant rollups, rejects malformed define, variant, and selected-variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, verifies define-forwarding metadata for every registered source frontend, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, applies named-variant define maps through OpenGL/GLSL native preprocessing during project translation, applies named-variant define maps through DirectX/HLSL native preprocessing during project translation, applies named-variant define maps through Metal/MSL native preprocessing during project translation, applies named-variant define maps through CUDA/HIP native preprocessing during project translation, applies named-variant define maps through Slang native preprocessing during project translation, applies named-variant define maps through Vulkan native preprocessing during project translation, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports configured selected_variants defaults plus API, scan/report CLI, and translation CLI scoped runs for selected declared variants, de-duplicates repeated selections before scan or artifact planning, and does not claim omitted variants as attempted, and sanitizes unsafe variant names into stable output path segments while retaining original variant labels in reports and validation rollups. Native preprocessor semantics remain backend-dependent, so this remains partial project support. Project inspection exposes project-level define names and deterministic define fingerprints, variant define names, and deterministic per-variant define fingerprints without define values. Define-processing inspection summaries expose redacted project define names, deterministic define fingerprints, selected variant names, per-variant define records, and artifact effective define fingerprints without define values."
   "Slang", "project", "Macro and define variants", "partial", "Define maps and named variants are planned, reported, validated, and covered across every registered source frontend as forwarded or explicitly not-supported, but native macro and preprocessor semantics remain backend-dependent.", "Document and expand backend-native macro semantic coverage, including macro forms that are translated, preserved, or rejected with diagnostics for each source frontend.", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, selected-variant lists, and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, emits scan-time diagnostics for active #error and #warning directives scoped by project and selected variant conditionals, emits warning diagnostics when active #define or #undef directives in translation units or resolved include files shadow configured project or selected variant define names while ignoring block-commented directives, inspection summaries and text output expose variant names, selected-variant summaries for scoped runs, and per-variant define counts without define values plus define-processing status, target, source-backend, and variant rollups plus sampled define-processing artifact metadata with define names, deterministic define fingerprints, and without define values, validates malformed define/variant metadata including empty mapping keys, punctuation-bearing variant keys in config/report diagnostics, and forged variant define counts, duplicate selected variant metadata, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including target and variant rollups, missing define-processing target and variant rollups, rejects malformed define, variant, and selected-variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, verifies define-forwarding metadata for every registered source frontend, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, applies named-variant define maps through OpenGL/GLSL native preprocessing during project translation, applies named-variant define maps through DirectX/HLSL native preprocessing during project translation, applies named-variant define maps through Metal/MSL native preprocessing during project translation, applies named-variant define maps through CUDA/HIP native preprocessing during project translation, applies named-variant define maps through Slang native preprocessing during project translation, applies named-variant define maps through Vulkan native preprocessing during project translation, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports configured selected_variants defaults plus API, scan/report CLI, and translation CLI scoped runs for selected declared variants, de-duplicates repeated selections before scan or artifact planning, and does not claim omitted variants as attempted, and sanitizes unsafe variant names into stable output path segments while retaining original variant labels in reports and validation rollups. Native preprocessor semantics remain backend-dependent, so this remains partial project support. Project inspection exposes project-level define names and deterministic define fingerprints, variant define names, and deterministic per-variant define fingerprints without define values. Define-processing inspection summaries expose redacted project define names, deterministic define fingerprints, selected variant names, per-variant define records, and artifact effective define fingerprints without define values."
   "DirectX / HLSL", "project", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data.", "Project reports include configured source override maps, unit source hashes and byte sizes, artifact source hashes and byte sizes, generated artifact hashes and byte sizes, relative paths, source backend names, intermediate CrossGL provenance, file-span artifact source-map anchors, line-preserving source-map mappings, and line-preserving source-map validation with expected/actual line-preserving mapping mismatch context, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, including fine-grained totals derived from non-file map granularity, source-remap summary totals for artifact count and mapping count plus rollups by granularity, target, source backend, and variant, sanitized variant output segments in source-map and source-remap paths while retaining original variant labels in reports, stable repository-relative POSIX report identity paths for source provenance and source-remap metadata, canonical source-map and source-remap target metadata, source-map, source-remap target, source-remap mapping-count, and source-remap sidecar hash and byte-size metadata inspection, bounded source-map and source-remap artifact inspection samples with source/generated span, mapping-count, source and generated hash and byte-size metadata, sidecar hash plus byte-size metadata, failed validation status metadata, and custom limits, and text inspection derives file-level source-map counts from total and fine-grained source-map totals and reports aggregate source-remap mapping counts, artifact provenance summary rollups by pipeline, intermediate marker, source backend with intermediate marker, target with intermediate marker, and variant with intermediate marker, bounded direct, bridged, and combined artifact provenance inspection samples with source hash and byte-size metadata plus generated hash and byte-size metadata, failed validation status metadata, and custom limits, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and source-map-derived generated/original mappings for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, source-size, generated hash, generated-size, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts including source-size and generated-size counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance with expected/actual source override provenance mismatch context, unit source hash records that are missing, malformed, or no longer match the current source file, unit source byte-size records that are missing, malformed, or no longer match the current source file, with expected/actual unit source hash and byte-size mismatch context, artifact source byte-size records that are missing, malformed, or inconsistent with declared units, with expected/actual artifact source hash and byte-size mismatch context, source-size validation records that no longer match the current source artifact, artifact generated byte-size records that are missing or malformed, generated-size validation records that no longer match the current generated artifact, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, with expected/actual artifact identity mismatch context, source-map records, closed source-map field sets, non-empty source-map mappings, file-level source-map mapping cardinality, fine-grained positive-length source-map mappings, fine-grained source-map byte and line/column span containment, source-map span consistency, source-map anchor consistency, expected/actual source-map and source-remap mismatch context, current artifact-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, non-CrossGL target artifacts with source-remap metadata, source-remap summary rollups by granularity and including variant rollups, missing source-map and source-remap granularity or variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, source-remap mapping-count summary mismatches, source-remap mapping-count mismatches, source-remap sidecar size mismatches, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. SARIF remapped diagnostics with sanitized diagnosticLocation and originalLocation result properties. Generated line source-map mappings are available for line-preserving artifacts; backend-generated line, statement, and token source-map mappings remain unavailable in the translator project pipeline."
   "OpenGL / GLSL", "project", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data.", "Project reports include configured source override maps, unit source hashes and byte sizes, artifact source hashes and byte sizes, generated artifact hashes and byte sizes, relative paths, source backend names, intermediate CrossGL provenance, file-span artifact source-map anchors, line-preserving source-map mappings, and line-preserving source-map validation with expected/actual line-preserving mapping mismatch context, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, including fine-grained totals derived from non-file map granularity, source-remap summary totals for artifact count and mapping count plus rollups by granularity, target, source backend, and variant, sanitized variant output segments in source-map and source-remap paths while retaining original variant labels in reports, stable repository-relative POSIX report identity paths for source provenance and source-remap metadata, canonical source-map and source-remap target metadata, source-map, source-remap target, source-remap mapping-count, and source-remap sidecar hash and byte-size metadata inspection, bounded source-map and source-remap artifact inspection samples with source/generated span, mapping-count, source and generated hash and byte-size metadata, sidecar hash plus byte-size metadata, failed validation status metadata, and custom limits, and text inspection derives file-level source-map counts from total and fine-grained source-map totals and reports aggregate source-remap mapping counts, artifact provenance summary rollups by pipeline, intermediate marker, source backend with intermediate marker, target with intermediate marker, and variant with intermediate marker, bounded direct, bridged, and combined artifact provenance inspection samples with source hash and byte-size metadata plus generated hash and byte-size metadata, failed validation status metadata, and custom limits, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and source-map-derived generated/original mappings for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, source-size, generated hash, generated-size, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts including source-size and generated-size counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance with expected/actual source override provenance mismatch context, unit source hash records that are missing, malformed, or no longer match the current source file, unit source byte-size records that are missing, malformed, or no longer match the current source file, with expected/actual unit source hash and byte-size mismatch context, artifact source byte-size records that are missing, malformed, or inconsistent with declared units, with expected/actual artifact source hash and byte-size mismatch context, source-size validation records that no longer match the current source artifact, artifact generated byte-size records that are missing or malformed, generated-size validation records that no longer match the current generated artifact, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, with expected/actual artifact identity mismatch context, source-map records, closed source-map field sets, non-empty source-map mappings, file-level source-map mapping cardinality, fine-grained positive-length source-map mappings, fine-grained source-map byte and line/column span containment, source-map span consistency, source-map anchor consistency, expected/actual source-map and source-remap mismatch context, current artifact-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, non-CrossGL target artifacts with source-remap metadata, source-remap summary rollups by granularity and including variant rollups, missing source-map and source-remap granularity or variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, source-remap mapping-count summary mismatches, source-remap mapping-count mismatches, source-remap sidecar size mismatches, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. SARIF remapped diagnostics with sanitized diagnosticLocation and originalLocation result properties. Generated line source-map mappings are available for line-preserving artifacts; backend-generated line, statement, and token source-map mappings remain unavailable in the translator project pipeline."
   "Metal", "project", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data.", "Project reports include configured source override maps, unit source hashes and byte sizes, artifact source hashes and byte sizes, generated artifact hashes and byte sizes, relative paths, source backend names, intermediate CrossGL provenance, file-span artifact source-map anchors, line-preserving source-map mappings, and line-preserving source-map validation with expected/actual line-preserving mapping mismatch context, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, including fine-grained totals derived from non-file map granularity, source-remap summary totals for artifact count and mapping count plus rollups by granularity, target, source backend, and variant, sanitized variant output segments in source-map and source-remap paths while retaining original variant labels in reports, stable repository-relative POSIX report identity paths for source provenance and source-remap metadata, canonical source-map and source-remap target metadata, source-map, source-remap target, source-remap mapping-count, and source-remap sidecar hash and byte-size metadata inspection, bounded source-map and source-remap artifact inspection samples with source/generated span, mapping-count, source and generated hash and byte-size metadata, sidecar hash plus byte-size metadata, failed validation status metadata, and custom limits, and text inspection derives file-level source-map counts from total and fine-grained source-map totals and reports aggregate source-remap mapping counts, artifact provenance summary rollups by pipeline, intermediate marker, source backend with intermediate marker, target with intermediate marker, and variant with intermediate marker, bounded direct, bridged, and combined artifact provenance inspection samples with source hash and byte-size metadata plus generated hash and byte-size metadata, failed validation status metadata, and custom limits, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and source-map-derived generated/original mappings for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, source-size, generated hash, generated-size, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts including source-size and generated-size counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance with expected/actual source override provenance mismatch context, unit source hash records that are missing, malformed, or no longer match the current source file, unit source byte-size records that are missing, malformed, or no longer match the current source file, with expected/actual unit source hash and byte-size mismatch context, artifact source byte-size records that are missing, malformed, or inconsistent with declared units, with expected/actual artifact source hash and byte-size mismatch context, source-size validation records that no longer match the current source artifact, artifact generated byte-size records that are missing or malformed, generated-size validation records that no longer match the current generated artifact, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, with expected/actual artifact identity mismatch context, source-map records, closed source-map field sets, non-empty source-map mappings, file-level source-map mapping cardinality, fine-grained positive-length source-map mappings, fine-grained source-map byte and line/column span containment, source-map span consistency, source-map anchor consistency, expected/actual source-map and source-remap mismatch context, current artifact-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, non-CrossGL target artifacts with source-remap metadata, source-remap summary rollups by granularity and including variant rollups, missing source-map and source-remap granularity or variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, source-remap mapping-count summary mismatches, source-remap mapping-count mismatches, source-remap sidecar size mismatches, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. SARIF remapped diagnostics with sanitized diagnosticLocation and originalLocation result properties. Generated line source-map mappings are available for line-preserving artifacts; backend-generated line, statement, and token source-map mappings remain unavailable in the translator project pipeline."
   "Vulkan SPIR-V", "project", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data.", "Project reports include configured source override maps, unit source hashes and byte sizes, artifact source hashes and byte sizes, generated artifact hashes and byte sizes, relative paths, source backend names, intermediate CrossGL provenance, file-span artifact source-map anchors, line-preserving source-map mappings, and line-preserving source-map validation with expected/actual line-preserving mapping mismatch context, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, including fine-grained totals derived from non-file map granularity, source-remap summary totals for artifact count and mapping count plus rollups by granularity, target, source backend, and variant, sanitized variant output segments in source-map and source-remap paths while retaining original variant labels in reports, stable repository-relative POSIX report identity paths for source provenance and source-remap metadata, canonical source-map and source-remap target metadata, source-map, source-remap target, source-remap mapping-count, and source-remap sidecar hash and byte-size metadata inspection, bounded source-map and source-remap artifact inspection samples with source/generated span, mapping-count, source and generated hash and byte-size metadata, sidecar hash plus byte-size metadata, failed validation status metadata, and custom limits, and text inspection derives file-level source-map counts from total and fine-grained source-map totals and reports aggregate source-remap mapping counts, artifact provenance summary rollups by pipeline, intermediate marker, source backend with intermediate marker, target with intermediate marker, and variant with intermediate marker, bounded direct, bridged, and combined artifact provenance inspection samples with source hash and byte-size metadata plus generated hash and byte-size metadata, failed validation status metadata, and custom limits, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and source-map-derived generated/original mappings for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, source-size, generated hash, generated-size, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts including source-size and generated-size counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance with expected/actual source override provenance mismatch context, unit source hash records that are missing, malformed, or no longer match the current source file, unit source byte-size records that are missing, malformed, or no longer match the current source file, with expected/actual unit source hash and byte-size mismatch context, artifact source byte-size records that are missing, malformed, or inconsistent with declared units, with expected/actual artifact source hash and byte-size mismatch context, source-size validation records that no longer match the current source artifact, artifact generated byte-size records that are missing or malformed, generated-size validation records that no longer match the current generated artifact, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, with expected/actual artifact identity mismatch context, source-map records, closed source-map field sets, non-empty source-map mappings, file-level source-map mapping cardinality, fine-grained positive-length source-map mappings, fine-grained source-map byte and line/column span containment, source-map span consistency, source-map anchor consistency, expected/actual source-map and source-remap mismatch context, current artifact-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, non-CrossGL target artifacts with source-remap metadata, source-remap summary rollups by granularity and including variant rollups, missing source-map and source-remap granularity or variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, source-remap mapping-count summary mismatches, source-remap mapping-count mismatches, source-remap sidecar size mismatches, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. SARIF remapped diagnostics with sanitized diagnosticLocation and originalLocation result properties. Generated line source-map mappings are available for line-preserving artifacts; backend-generated line, statement, and token source-map mappings remain unavailable in the translator project pipeline."
   "CUDA", "project", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data.", "Project reports include configured source override maps, unit source hashes and byte sizes, artifact source hashes and byte sizes, generated artifact hashes and byte sizes, relative paths, source backend names, intermediate CrossGL provenance, file-span artifact source-map anchors, line-preserving source-map mappings, and line-preserving source-map validation with expected/actual line-preserving mapping mismatch context, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, including fine-grained totals derived from non-file map granularity, source-remap summary totals for artifact count and mapping count plus rollups by granularity, target, source backend, and variant, sanitized variant output segments in source-map and source-remap paths while retaining original variant labels in reports, stable repository-relative POSIX report identity paths for source provenance and source-remap metadata, canonical source-map and source-remap target metadata, source-map, source-remap target, source-remap mapping-count, and source-remap sidecar hash and byte-size metadata inspection, bounded source-map and source-remap artifact inspection samples with source/generated span, mapping-count, source and generated hash and byte-size metadata, sidecar hash plus byte-size metadata, failed validation status metadata, and custom limits, and text inspection derives file-level source-map counts from total and fine-grained source-map totals and reports aggregate source-remap mapping counts, artifact provenance summary rollups by pipeline, intermediate marker, source backend with intermediate marker, target with intermediate marker, and variant with intermediate marker, bounded direct, bridged, and combined artifact provenance inspection samples with source hash and byte-size metadata plus generated hash and byte-size metadata, failed validation status metadata, and custom limits, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and source-map-derived generated/original mappings for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, source-size, generated hash, generated-size, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts including source-size and generated-size counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance with expected/actual source override provenance mismatch context, unit source hash records that are missing, malformed, or no longer match the current source file, unit source byte-size records that are missing, malformed, or no longer match the current source file, with expected/actual unit source hash and byte-size mismatch context, artifact source byte-size records that are missing, malformed, or inconsistent with declared units, with expected/actual artifact source hash and byte-size mismatch context, source-size validation records that no longer match the current source artifact, artifact generated byte-size records that are missing or malformed, generated-size validation records that no longer match the current generated artifact, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, with expected/actual artifact identity mismatch context, source-map records, closed source-map field sets, non-empty source-map mappings, file-level source-map mapping cardinality, fine-grained positive-length source-map mappings, fine-grained source-map byte and line/column span containment, source-map span consistency, source-map anchor consistency, expected/actual source-map and source-remap mismatch context, current artifact-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, non-CrossGL target artifacts with source-remap metadata, source-remap summary rollups by granularity and including variant rollups, missing source-map and source-remap granularity or variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, source-remap mapping-count summary mismatches, source-remap mapping-count mismatches, source-remap sidecar size mismatches, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. SARIF remapped diagnostics with sanitized diagnosticLocation and originalLocation result properties. Generated line source-map mappings are available for line-preserving artifacts; backend-generated line, statement, and token source-map mappings remain unavailable in the translator project pipeline."
   "HIP", "project", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data.", "Project reports include configured source override maps, unit source hashes and byte sizes, artifact source hashes and byte sizes, generated artifact hashes and byte sizes, relative paths, source backend names, intermediate CrossGL provenance, file-span artifact source-map anchors, line-preserving source-map mappings, and line-preserving source-map validation with expected/actual line-preserving mapping mismatch context, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, including fine-grained totals derived from non-file map granularity, source-remap summary totals for artifact count and mapping count plus rollups by granularity, target, source backend, and variant, sanitized variant output segments in source-map and source-remap paths while retaining original variant labels in reports, stable repository-relative POSIX report identity paths for source provenance and source-remap metadata, canonical source-map and source-remap target metadata, source-map, source-remap target, source-remap mapping-count, and source-remap sidecar hash and byte-size metadata inspection, bounded source-map and source-remap artifact inspection samples with source/generated span, mapping-count, source and generated hash and byte-size metadata, sidecar hash plus byte-size metadata, failed validation status metadata, and custom limits, and text inspection derives file-level source-map counts from total and fine-grained source-map totals and reports aggregate source-remap mapping counts, artifact provenance summary rollups by pipeline, intermediate marker, source backend with intermediate marker, target with intermediate marker, and variant with intermediate marker, bounded direct, bridged, and combined artifact provenance inspection samples with source hash and byte-size metadata plus generated hash and byte-size metadata, failed validation status metadata, and custom limits, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and source-map-derived generated/original mappings for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, source-size, generated hash, generated-size, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts including source-size and generated-size counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance with expected/actual source override provenance mismatch context, unit source hash records that are missing, malformed, or no longer match the current source file, unit source byte-size records that are missing, malformed, or no longer match the current source file, with expected/actual unit source hash and byte-size mismatch context, artifact source byte-size records that are missing, malformed, or inconsistent with declared units, with expected/actual artifact source hash and byte-size mismatch context, source-size validation records that no longer match the current source artifact, artifact generated byte-size records that are missing or malformed, generated-size validation records that no longer match the current generated artifact, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, with expected/actual artifact identity mismatch context, source-map records, closed source-map field sets, non-empty source-map mappings, file-level source-map mapping cardinality, fine-grained positive-length source-map mappings, fine-grained source-map byte and line/column span containment, source-map span consistency, source-map anchor consistency, expected/actual source-map and source-remap mismatch context, current artifact-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, non-CrossGL target artifacts with source-remap metadata, source-remap summary rollups by granularity and including variant rollups, missing source-map and source-remap granularity or variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, source-remap mapping-count summary mismatches, source-remap mapping-count mismatches, source-remap sidecar size mismatches, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. SARIF remapped diagnostics with sanitized diagnosticLocation and originalLocation result properties. Generated line source-map mappings are available for line-preserving artifacts; backend-generated line, statement, and token source-map mappings remain unavailable in the translator project pipeline."
   "Mojo", "project", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data.", "Project reports include configured source override maps, unit source hashes and byte sizes, artifact source hashes and byte sizes, generated artifact hashes and byte sizes, relative paths, source backend names, intermediate CrossGL provenance, file-span artifact source-map anchors, line-preserving source-map mappings, and line-preserving source-map validation with expected/actual line-preserving mapping mismatch context, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, including fine-grained totals derived from non-file map granularity, source-remap summary totals for artifact count and mapping count plus rollups by granularity, target, source backend, and variant, sanitized variant output segments in source-map and source-remap paths while retaining original variant labels in reports, stable repository-relative POSIX report identity paths for source provenance and source-remap metadata, canonical source-map and source-remap target metadata, source-map, source-remap target, source-remap mapping-count, and source-remap sidecar hash and byte-size metadata inspection, bounded source-map and source-remap artifact inspection samples with source/generated span, mapping-count, source and generated hash and byte-size metadata, sidecar hash plus byte-size metadata, failed validation status metadata, and custom limits, and text inspection derives file-level source-map counts from total and fine-grained source-map totals and reports aggregate source-remap mapping counts, artifact provenance summary rollups by pipeline, intermediate marker, source backend with intermediate marker, target with intermediate marker, and variant with intermediate marker, bounded direct, bridged, and combined artifact provenance inspection samples with source hash and byte-size metadata plus generated hash and byte-size metadata, failed validation status metadata, and custom limits, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and source-map-derived generated/original mappings for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, source-size, generated hash, generated-size, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts including source-size and generated-size counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance with expected/actual source override provenance mismatch context, unit source hash records that are missing, malformed, or no longer match the current source file, unit source byte-size records that are missing, malformed, or no longer match the current source file, with expected/actual unit source hash and byte-size mismatch context, artifact source byte-size records that are missing, malformed, or inconsistent with declared units, with expected/actual artifact source hash and byte-size mismatch context, source-size validation records that no longer match the current source artifact, artifact generated byte-size records that are missing or malformed, generated-size validation records that no longer match the current generated artifact, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, with expected/actual artifact identity mismatch context, source-map records, closed source-map field sets, non-empty source-map mappings, file-level source-map mapping cardinality, fine-grained positive-length source-map mappings, fine-grained source-map byte and line/column span containment, source-map span consistency, source-map anchor consistency, expected/actual source-map and source-remap mismatch context, current artifact-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, non-CrossGL target artifacts with source-remap metadata, source-remap summary rollups by granularity and including variant rollups, missing source-map and source-remap granularity or variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, source-remap mapping-count summary mismatches, source-remap mapping-count mismatches, source-remap sidecar size mismatches, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. SARIF remapped diagnostics with sanitized diagnosticLocation and originalLocation result properties. Generated line source-map mappings are available for line-preserving artifacts; backend-generated line, statement, and token source-map mappings remain unavailable in the translator project pipeline."
   "Rust", "project", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data.", "Project reports include configured source override maps, unit source hashes and byte sizes, artifact source hashes and byte sizes, generated artifact hashes and byte sizes, relative paths, source backend names, intermediate CrossGL provenance, file-span artifact source-map anchors, line-preserving source-map mappings, and line-preserving source-map validation with expected/actual line-preserving mapping mismatch context, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, including fine-grained totals derived from non-file map granularity, source-remap summary totals for artifact count and mapping count plus rollups by granularity, target, source backend, and variant, sanitized variant output segments in source-map and source-remap paths while retaining original variant labels in reports, stable repository-relative POSIX report identity paths for source provenance and source-remap metadata, canonical source-map and source-remap target metadata, source-map, source-remap target, source-remap mapping-count, and source-remap sidecar hash and byte-size metadata inspection, bounded source-map and source-remap artifact inspection samples with source/generated span, mapping-count, source and generated hash and byte-size metadata, sidecar hash plus byte-size metadata, failed validation status metadata, and custom limits, and text inspection derives file-level source-map counts from total and fine-grained source-map totals and reports aggregate source-remap mapping counts, artifact provenance summary rollups by pipeline, intermediate marker, source backend with intermediate marker, target with intermediate marker, and variant with intermediate marker, bounded direct, bridged, and combined artifact provenance inspection samples with source hash and byte-size metadata plus generated hash and byte-size metadata, failed validation status metadata, and custom limits, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and source-map-derived generated/original mappings for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, source-size, generated hash, generated-size, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts including source-size and generated-size counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance with expected/actual source override provenance mismatch context, unit source hash records that are missing, malformed, or no longer match the current source file, unit source byte-size records that are missing, malformed, or no longer match the current source file, with expected/actual unit source hash and byte-size mismatch context, artifact source byte-size records that are missing, malformed, or inconsistent with declared units, with expected/actual artifact source hash and byte-size mismatch context, source-size validation records that no longer match the current source artifact, artifact generated byte-size records that are missing or malformed, generated-size validation records that no longer match the current generated artifact, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, with expected/actual artifact identity mismatch context, source-map records, closed source-map field sets, non-empty source-map mappings, file-level source-map mapping cardinality, fine-grained positive-length source-map mappings, fine-grained source-map byte and line/column span containment, source-map span consistency, source-map anchor consistency, expected/actual source-map and source-remap mismatch context, current artifact-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, non-CrossGL target artifacts with source-remap metadata, source-remap summary rollups by granularity and including variant rollups, missing source-map and source-remap granularity or variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, source-remap mapping-count summary mismatches, source-remap mapping-count mismatches, source-remap sidecar size mismatches, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. SARIF remapped diagnostics with sanitized diagnosticLocation and originalLocation result properties. Generated line source-map mappings are available for line-preserving artifacts; backend-generated line, statement, and token source-map mappings remain unavailable in the translator project pipeline."
   "Slang", "project", "Source provenance and source-map bridge", "partial", "Project artifacts carry file-level source maps for generated artifacts, line-granularity source maps for line-preserving artifacts, compiler-compatible source-remap sidecars, and source-map rollups correctly account for non-file map granularity, but backend-generated line, statement, and token mappings are not implemented.", "Add backend-generated line, statement, or token mappings and validation checks for translated artifacts when backend pipelines expose enough span data.", "Project reports include configured source override maps, unit source hashes and byte sizes, artifact source hashes and byte sizes, generated artifact hashes and byte sizes, relative paths, source backend names, intermediate CrossGL provenance, file-span artifact source-map anchors, line-preserving source-map mappings, and line-preserving source-map validation with expected/actual line-preserving mapping mismatch context, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, including fine-grained totals derived from non-file map granularity, source-remap summary totals for artifact count and mapping count plus rollups by granularity, target, source backend, and variant, sanitized variant output segments in source-map and source-remap paths while retaining original variant labels in reports, stable repository-relative POSIX report identity paths for source provenance and source-remap metadata, canonical source-map and source-remap target metadata, source-map, source-remap target, source-remap mapping-count, and source-remap sidecar hash and byte-size metadata inspection, bounded source-map and source-remap artifact inspection samples with source/generated span, mapping-count, source and generated hash and byte-size metadata, sidecar hash plus byte-size metadata, failed validation status metadata, and custom limits, and text inspection derives file-level source-map counts from total and fine-grained source-map totals and reports aggregate source-remap mapping counts, artifact provenance summary rollups by pipeline, intermediate marker, source backend with intermediate marker, target with intermediate marker, and variant with intermediate marker, bounded direct, bridged, and combined artifact provenance inspection samples with source hash and byte-size metadata plus generated hash and byte-size metadata, failed validation status metadata, and custom limits, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and source-map-derived generated/original mappings for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, source-size, generated hash, generated-size, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts including source-size and generated-size counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance with expected/actual source override provenance mismatch context, unit source hash records that are missing, malformed, or no longer match the current source file, unit source byte-size records that are missing, malformed, or no longer match the current source file, with expected/actual unit source hash and byte-size mismatch context, artifact source byte-size records that are missing, malformed, or inconsistent with declared units, with expected/actual artifact source hash and byte-size mismatch context, source-size validation records that no longer match the current source artifact, artifact generated byte-size records that are missing or malformed, generated-size validation records that no longer match the current generated artifact, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, with expected/actual artifact identity mismatch context, source-map records, closed source-map field sets, non-empty source-map mappings, file-level source-map mapping cardinality, fine-grained positive-length source-map mappings, fine-grained source-map byte and line/column span containment, source-map span consistency, source-map anchor consistency, expected/actual source-map and source-remap mismatch context, current artifact-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, non-CrossGL target artifacts with source-remap metadata, source-remap summary rollups by granularity and including variant rollups, missing source-map and source-remap granularity or variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, source-remap mapping-count summary mismatches, source-remap mapping-count mismatches, source-remap sidecar size mismatches, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. SARIF remapped diagnostics with sanitized diagnosticLocation and originalLocation result properties. Generated line source-map mappings are available for line-preserving artifacts; backend-generated line, statement, and token source-map mappings remain unavailable in the translator project pipeline."

Documentation Sources
---------------------

The nightly documentation probe checks these URLs for reachability and
content hash changes. It does not overwrite feature statuses; status
changes must be reviewed and committed in the source catalog.

.. csv-table:: Official documentation URLs
   :header: "Backend", "Source", "URL"

   "DirectX / HLSL", "Microsoft Learn HLSL reference", "https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx-graphics-hlsl-reference"
   "DirectX / HLSL", "HLSL specification project", "https://microsoft.github.io/hlsl-specs/"
   "OpenGL / GLSL", "GLSL 4.60 specification", "https://raw.githubusercontent.com/KhronosGroup/OpenGL-Registry/main/specs/gl/GLSLangSpec.4.60.html"
   "OpenGL / GLSL", "OpenGL registry", "https://github.com/KhronosGroup/OpenGL-Registry"
   "Metal", "Apple Metal resources", "https://developer.apple.com/metal/resources/"
   "Metal", "Metal Shading Language specification", "https://developer.apple.com/metal/Metal-Shading-Language-Specification.pdf"
   "Vulkan SPIR-V", "SPIR-V unified grammar", "https://raw.githubusercontent.com/KhronosGroup/SPIRV-Headers/main/include/spirv/unified1/spirv.core.grammar.json"
   "Vulkan SPIR-V", "Khronos SPIR-V headers", "https://github.com/KhronosGroup/SPIRV-Headers"
   "CUDA", "CUDA C++ programming guide", "https://docs.nvidia.com/cuda/cuda-c-programming-guide/"
   "HIP", "ROCm HIP documentation", "https://rocm.docs.amd.com/projects/HIP/en/latest/"
   "Mojo", "Mojo manual", "https://mojolang.org/docs/manual/"
   "Rust", "Rust reference", "https://doc.rust-lang.org/reference/"
   "Slang", "Slang user guide", "https://shader-slang.org/slang/user-guide/"
