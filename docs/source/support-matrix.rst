Support Matrix
==============

.. This file is generated by tools/support_matrix.py. Do not edit by hand.

This matrix tracks CrossGL backend coverage from the checked-in support
catalog. A feature is marked supported only when the repo has implementation
and test evidence for the CrossGL contract. Unknown means unaudited, not
implicitly supported.

.. csv-table:: Status legend
   :header: "Code", "Status", "Meaning"
   :widths: 8, 20, 72

   "Y", "supported", "Implemented with positive tests for the repo-owned CrossGL contract."
   "P", "partial", "Implemented for common cases, but known target features or edge cases are missing."
   "D", "diagnostic", "The backend emits a deterministic diagnostic or fallback instead of full target code."
   "R", "validated_rejection", "The backend rejects this shape with an explicit validation error."
   "U", "unsupported", "The backend intentionally does not support this capability yet."
   "?", "unknown", "Not audited yet. Do not treat this as supported."

.. csv-table:: Backend inventory
   :header: "Backend", "Ext", "Target generator", "Native frontend", "Tests", "Test count", "Unsupported markers", "Docs source"

   "DirectX / HLSL", ".hlsl", "crosstl/translator/codegen/directx_codegen.py", "crosstl/backend/DirectX", "tests/test_translator/test_codegen/test_directx_codegen.py, tests/test_backend/test_directx", "895", "291", "Microsoft Learn HLSL reference; HLSL specification project"
   "OpenGL / GLSL", ".glsl", "crosstl/translator/codegen/GLSL_codegen.py", "crosstl/backend/GLSL", "tests/test_translator/test_codegen/test_GLSL_codegen.py, tests/test_backend/test_GLSL", "1004", "176", "GLSL 4.60 specification; OpenGL registry"
   "Metal", ".metal", "crosstl/translator/codegen/metal_codegen.py", "crosstl/backend/Metal", "tests/test_translator/test_codegen/test_metal_codegen.py, tests/test_backend/test_metal", "786", "480", "Apple Metal resources; Metal Shading Language specification"
   "Vulkan SPIR-V", ".spvasm", "crosstl/translator/codegen/SPIRV_codegen.py", "crosstl/backend/SPIRV", "tests/test_translator/test_codegen/test_SPIRV_codegen.py, tests/test_backend/test_SPIRV", "793", "33", "SPIR-V unified grammar; Khronos SPIR-V headers"
   "CUDA", ".cu", "crosstl/translator/codegen/cuda_codegen.py", "crosstl/backend/CUDA", "tests/test_translator/test_codegen/test_CUDA_codegen.py, tests/test_backend/test_CUDA", "602", "175", "CUDA C++ programming guide"
   "HIP", ".hip", "crosstl/translator/codegen/hip_codegen.py", "crosstl/backend/HIP", "tests/test_translator/test_codegen/test_hip_codegen.py, tests/test_backend/test_HIP", "648", "115", "ROCm HIP documentation"
   "Mojo", ".mojo", "crosstl/translator/codegen/mojo_codegen.py", "crosstl/backend/Mojo", "tests/test_translator/test_codegen/test_mojo_codegen.py, tests/test_backend/test_mojo", "703", "97", "Mojo manual"
   "Rust", ".rs", "crosstl/translator/codegen/rust_codegen.py", "crosstl/backend/Rust", "tests/test_translator/test_codegen/test_rust_codegen.py, tests/test_backend/test_rust", "778", "69", "Rust reference"
   "Slang", ".slang", "crosstl/translator/codegen/slang_codegen.py", "crosstl/backend/slang", "tests/test_translator/test_codegen/test_slang_codegen.py, tests/test_backend/test_slang", "635", "402", "Slang user guide"

.. csv-table:: Summary by backend
   :header: "Backend", "supported", "partial", "diagnostic", "validated_rejection", "unsupported", "unknown"

   "DirectX / HLSL", "46", "5", "2", "0", "0", "0"
   "OpenGL / GLSL", "46", "5", "2", "0", "0", "0"
   "Metal", "45", "5", "3", "0", "0", "0"
   "Vulkan SPIR-V", "46", "5", "2", "0", "0", "0"
   "CUDA", "41", "5", "7", "0", "0", "0"
   "HIP", "41", "5", "7", "0", "0", "0"
   "Mojo", "43", "5", "5", "0", "0", "0"
   "Rust", "44", "5", "4", "0", "0", "0"
   "Slang", "45", "5", "3", "0", "0", "0"

Graphics Backend Focus
----------------------

This view isolates the DirectX, OpenGL, and Metal rows that are in
scope for graphics backend completion work.

.. csv-table:: Graphics backend status summary
   :header: "Backend", "supported", "partial", "diagnostic", "validated_rejection", "unsupported", "unknown"

   "DirectX / HLSL", "46", "5", "2", "0", "0", "0"
   "OpenGL / GLSL", "46", "5", "2", "0", "0", "0"
   "Metal", "45", "5", "3", "0", "0", "0"

.. csv-table:: DirectX/OpenGL/Metal backlog
   :header: "Backend", "Category", "Feature", "Status", "Notes"

   "DirectX / HLSL", "project", "Project include resolution", "partial", "The project pipeline records include directories, reports missing include directories, reports include directories that resolve outside the repository, applies CLI include-directory overrides, and passes resolved paths to source frontends that expose include-path preprocessor options. Include semantics remain backend-dependent, so this remains partial project support."
   "OpenGL / GLSL", "project", "Project include resolution", "partial", "The project pipeline records include directories, reports missing include directories, reports include directories that resolve outside the repository, applies CLI include-directory overrides, and passes resolved paths to source frontends that expose include-path preprocessor options. Include semantics remain backend-dependent, so this remains partial project support."
   "Metal", "project", "Project include resolution", "partial", "The project pipeline records include directories, reports missing include directories, reports include directories that resolve outside the repository, applies CLI include-directory overrides, and passes resolved paths to source frontends that expose include-path preprocessor options. Include semantics remain backend-dependent, so this remains partial project support."
   "DirectX / HLSL", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, validates malformed define/variant metadata and undeclared artifact variants in reports, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "OpenGL / GLSL", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, validates malformed define/variant metadata and undeclared artifact variants in reports, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "Metal", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, validates malformed define/variant metadata and undeclared artifact variants in reports, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "DirectX / HLSL", "project", "Batch project translation", "partial", "The project pipeline batches through the existing single-file translator, normalizes duplicate explicit targets, applies source backend overrides, records unsupported target diagnostics, preserves relative output paths, refuses to write artifacts outside the repository, and records failures per artifact. Full multi-corpus target coverage remains ongoing."
   "OpenGL / GLSL", "project", "Batch project translation", "partial", "The project pipeline batches through the existing single-file translator, normalizes duplicate explicit targets, applies source backend overrides, records unsupported target diagnostics, preserves relative output paths, refuses to write artifacts outside the repository, and records failures per artifact. Full multi-corpus target coverage remains ongoing."
   "Metal", "project", "Batch project translation", "partial", "The project pipeline batches through the existing single-file translator, normalizes duplicate explicit targets, applies source backend overrides, records unsupported target diagnostics, preserves relative output paths, refuses to write artifacts outside the repository, and records failures per artifact. Full multi-corpus target coverage remains ongoing."
   "DirectX / HLSL", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, source hashes, relative paths, source backend names, intermediate CrossGL provenance, and file-granularity artifact source-map anchors. Report validation accepts generated source maps, records per-artifact source hash status and aggregate hash-status counts, and rejects malformed project source override metadata, source-map records, inconsistent source-map anchors, source hash records, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "OpenGL / GLSL", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, source hashes, relative paths, source backend names, intermediate CrossGL provenance, and file-granularity artifact source-map anchors. Report validation accepts generated source maps, records per-artifact source hash status and aggregate hash-status counts, and rejects malformed project source override metadata, source-map records, inconsistent source-map anchors, source hash records, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "Metal", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, source hashes, relative paths, source backend names, intermediate CrossGL provenance, and file-granularity artifact source-map anchors. Report validation accepts generated source maps, records per-artifact source hash status and aggregate hash-status counts, and rejects malformed project source override metadata, source-map records, inconsistent source-map anchors, source hash records, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "DirectX / HLSL", "project", "External corpus coverage", "partial", "Project reports can attach a repository-relative external corpus manifest, skip malformed manifest entries with structured diagnostics, and summarize declared valid paths, present and missing entries, discovered translation units, source-backend, target, and artifact rollups for manifest-backed project runs, and text inspection surfaces the same rollups. The pinned reduced-corpus manifest includes a focused DirectX reduction covered by backend fixture tests. This is a coverage and accounting layer only: CrossTL does not clone upstream repositories, run native build systems, or claim whole-corpus semantic parity from reduced backend fixtures."
   "OpenGL / GLSL", "project", "External corpus coverage", "partial", "Project reports can attach a repository-relative external corpus manifest, skip malformed manifest entries with structured diagnostics, and summarize declared valid paths, present and missing entries, discovered translation units, source-backend, target, and artifact rollups for manifest-backed project runs, and text inspection surfaces the same rollups. The pinned reduced-corpus manifest includes a focused OpenGL reduction covered by backend fixture tests. This is a coverage and accounting layer only: CrossTL does not clone upstream repositories, run native build systems, or claim whole-corpus semantic parity from reduced backend fixtures."
   "Metal", "project", "External corpus coverage", "partial", "Project reports can attach a repository-relative external corpus manifest, skip malformed manifest entries with structured diagnostics, and summarize declared valid paths, present and missing entries, discovered translation units, source-backend, target, and artifact rollups for manifest-backed project runs, and text inspection surfaces the same rollups. The pinned reduced-corpus manifest includes a focused Metal reduction covered by backend fixture tests. This is a coverage and accounting layer only: CrossTL does not clone upstream repositories, run native build systems, or claim whole-corpus semantic parity from reduced backend fixtures."

Feature Matrix
--------------

Each category below uses the status codes from the legend.

.. csv-table:: target
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "CrossGL to target code generation", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: source
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Native source to CrossGL", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Native lexer coverage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Native preprocessor handling", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: stages
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Vertex stage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Fragment/pixel stage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Compute stage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Geometry stage", "Y", "Y", "D", "Y", "D", "D", "D", "D", "Y"
   "Tessellation stages", "Y", "D", "D", "Y", "D", "D", "D", "D", "Y"
   "Mesh/task/amplification stages", "Y", "Y", "Y", "Y", "D", "D", "D", "D", "D"
   "Ray tracing stages", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: stage I/O
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Stage parameter semantics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Direct function return semantics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Struct member semantics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: resources
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Explicit and automatic resource bindings", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Constant/uniform buffers", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Structured/storage buffers", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Resource arrays", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Texture and sampler object model", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "GLSL buffer block lowering", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Resource memory qualifiers", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: textures
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Texture sampling", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Texture LOD, gradient, and offset operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Projected texture operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Texture gather operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Shadow compare texture operations", "Y", "Y", "Y", "Y", "D", "D", "Y", "Y", "Y"
   "Texture query operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Texel fetch operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Multisample texture operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Advanced texture operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: images
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Storage image load/store", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Image atomics", "Y", "Y", "Y", "Y", "D", "D", "Y", "Y", "Y"
   "Multisample storage images", "D", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: language
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Struct declarations and construction", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Array declarations and access", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Function declarations and calls", "Y", "Y", "Y", "D", "D", "D", "D", "Y", "D"
   "Control flow", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Synchronization and memory barriers", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Wave/subgroup intrinsics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Match/pattern lowering", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Vector and matrix expressions", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Bitwise operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: validation
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Invalid shader shape validation", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: project
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Repository source discovery", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Project include resolution", "P", "P", "P", "P", "P", "P", "P", "P", "P"
   "Macro and define variants", "P", "P", "P", "P", "P", "P", "P", "P", "P"
   "Batch project translation", "P", "P", "P", "P", "P", "P", "P", "P", "P"
   "Optional validation hooks", "D", "D", "D", "D", "D", "D", "D", "D", "D"
   "Migration action report", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Artifact manifest", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Source provenance and source-map bridge", "P", "P", "P", "P", "P", "P", "P", "P", "P"
   "Project diagnostics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "External corpus coverage", "P", "P", "P", "P", "P", "P", "P", "P", "P"

Backlog
-------

These rows are the current path to full backend coverage. Unknown rows
need an audit before implementation work can be scoped accurately.

.. csv-table:: Non-supported or unaudited feature rows
   :header: "Backend", "Category", "Feature", "Status", "Notes"

   "DirectX / HLSL", "project", "Project include resolution", "partial", "The project pipeline records include directories, reports missing include directories, reports include directories that resolve outside the repository, applies CLI include-directory overrides, and passes resolved paths to source frontends that expose include-path preprocessor options. Include semantics remain backend-dependent, so this remains partial project support."
   "OpenGL / GLSL", "project", "Project include resolution", "partial", "The project pipeline records include directories, reports missing include directories, reports include directories that resolve outside the repository, applies CLI include-directory overrides, and passes resolved paths to source frontends that expose include-path preprocessor options. Include semantics remain backend-dependent, so this remains partial project support."
   "Metal", "project", "Project include resolution", "partial", "The project pipeline records include directories, reports missing include directories, reports include directories that resolve outside the repository, applies CLI include-directory overrides, and passes resolved paths to source frontends that expose include-path preprocessor options. Include semantics remain backend-dependent, so this remains partial project support."
   "Vulkan SPIR-V", "project", "Project include resolution", "partial", "The project pipeline records include directories, reports missing include directories, reports include directories that resolve outside the repository, applies CLI include-directory overrides, and passes resolved paths to source frontends that expose include-path preprocessor options. Include semantics remain backend-dependent, so this remains partial project support."
   "CUDA", "project", "Project include resolution", "partial", "The project pipeline records include directories, reports missing include directories, reports include directories that resolve outside the repository, applies CLI include-directory overrides, and passes resolved paths to source frontends that expose include-path preprocessor options. Include semantics remain backend-dependent, so this remains partial project support."
   "HIP", "project", "Project include resolution", "partial", "The project pipeline records include directories, reports missing include directories, reports include directories that resolve outside the repository, applies CLI include-directory overrides, and passes resolved paths to source frontends that expose include-path preprocessor options. Include semantics remain backend-dependent, so this remains partial project support."
   "Mojo", "project", "Project include resolution", "partial", "The project pipeline records include directories, reports missing include directories, reports include directories that resolve outside the repository, applies CLI include-directory overrides, and passes resolved paths to source frontends that expose include-path preprocessor options. Include semantics remain backend-dependent, so this remains partial project support."
   "Rust", "project", "Project include resolution", "partial", "The project pipeline records include directories, reports missing include directories, reports include directories that resolve outside the repository, applies CLI include-directory overrides, and passes resolved paths to source frontends that expose include-path preprocessor options. Include semantics remain backend-dependent, so this remains partial project support."
   "Slang", "project", "Project include resolution", "partial", "The project pipeline records include directories, reports missing include directories, reports include directories that resolve outside the repository, applies CLI include-directory overrides, and passes resolved paths to source frontends that expose include-path preprocessor options. Include semantics remain backend-dependent, so this remains partial project support."
   "DirectX / HLSL", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, validates malformed define/variant metadata and undeclared artifact variants in reports, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "OpenGL / GLSL", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, validates malformed define/variant metadata and undeclared artifact variants in reports, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "Metal", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, validates malformed define/variant metadata and undeclared artifact variants in reports, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "Vulkan SPIR-V", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, validates malformed define/variant metadata and undeclared artifact variants in reports, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "CUDA", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, validates malformed define/variant metadata and undeclared artifact variants in reports, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "HIP", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, validates malformed define/variant metadata and undeclared artifact variants in reports, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "Mojo", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, validates malformed define/variant metadata and undeclared artifact variants in reports, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "Rust", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, validates malformed define/variant metadata and undeclared artifact variants in reports, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "Slang", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts, validates malformed define/variant metadata and undeclared artifact variants in reports, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "DirectX / HLSL", "project", "Batch project translation", "partial", "The project pipeline batches through the existing single-file translator, normalizes duplicate explicit targets, applies source backend overrides, records unsupported target diagnostics, preserves relative output paths, refuses to write artifacts outside the repository, and records failures per artifact. Full multi-corpus target coverage remains ongoing."
   "OpenGL / GLSL", "project", "Batch project translation", "partial", "The project pipeline batches through the existing single-file translator, normalizes duplicate explicit targets, applies source backend overrides, records unsupported target diagnostics, preserves relative output paths, refuses to write artifacts outside the repository, and records failures per artifact. Full multi-corpus target coverage remains ongoing."
   "Metal", "project", "Batch project translation", "partial", "The project pipeline batches through the existing single-file translator, normalizes duplicate explicit targets, applies source backend overrides, records unsupported target diagnostics, preserves relative output paths, refuses to write artifacts outside the repository, and records failures per artifact. Full multi-corpus target coverage remains ongoing."
   "Vulkan SPIR-V", "project", "Batch project translation", "partial", "The project pipeline batches through the existing single-file translator, normalizes duplicate explicit targets, applies source backend overrides, records unsupported target diagnostics, preserves relative output paths, refuses to write artifacts outside the repository, and records failures per artifact. Full multi-corpus target coverage remains ongoing."
   "CUDA", "project", "Batch project translation", "partial", "The project pipeline batches through the existing single-file translator, normalizes duplicate explicit targets, applies source backend overrides, records unsupported target diagnostics, preserves relative output paths, refuses to write artifacts outside the repository, and records failures per artifact. Full multi-corpus target coverage remains ongoing."
   "HIP", "project", "Batch project translation", "partial", "The project pipeline batches through the existing single-file translator, normalizes duplicate explicit targets, applies source backend overrides, records unsupported target diagnostics, preserves relative output paths, refuses to write artifacts outside the repository, and records failures per artifact. Full multi-corpus target coverage remains ongoing."
   "Mojo", "project", "Batch project translation", "partial", "The project pipeline batches through the existing single-file translator, normalizes duplicate explicit targets, applies source backend overrides, records unsupported target diagnostics, preserves relative output paths, refuses to write artifacts outside the repository, and records failures per artifact. Full multi-corpus target coverage remains ongoing."
   "Rust", "project", "Batch project translation", "partial", "The project pipeline batches through the existing single-file translator, normalizes duplicate explicit targets, applies source backend overrides, records unsupported target diagnostics, preserves relative output paths, refuses to write artifacts outside the repository, and records failures per artifact. Full multi-corpus target coverage remains ongoing."
   "Slang", "project", "Batch project translation", "partial", "The project pipeline batches through the existing single-file translator, normalizes duplicate explicit targets, applies source backend overrides, records unsupported target diagnostics, preserves relative output paths, refuses to write artifacts outside the repository, and records failures per artifact. Full multi-corpus target coverage remains ongoing."
   "DirectX / HLSL", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, source hashes, relative paths, source backend names, intermediate CrossGL provenance, and file-granularity artifact source-map anchors. Report validation accepts generated source maps, records per-artifact source hash status and aggregate hash-status counts, and rejects malformed project source override metadata, source-map records, inconsistent source-map anchors, source hash records, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "OpenGL / GLSL", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, source hashes, relative paths, source backend names, intermediate CrossGL provenance, and file-granularity artifact source-map anchors. Report validation accepts generated source maps, records per-artifact source hash status and aggregate hash-status counts, and rejects malformed project source override metadata, source-map records, inconsistent source-map anchors, source hash records, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "Metal", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, source hashes, relative paths, source backend names, intermediate CrossGL provenance, and file-granularity artifact source-map anchors. Report validation accepts generated source maps, records per-artifact source hash status and aggregate hash-status counts, and rejects malformed project source override metadata, source-map records, inconsistent source-map anchors, source hash records, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "Vulkan SPIR-V", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, source hashes, relative paths, source backend names, intermediate CrossGL provenance, and file-granularity artifact source-map anchors. Report validation accepts generated source maps, records per-artifact source hash status and aggregate hash-status counts, and rejects malformed project source override metadata, source-map records, inconsistent source-map anchors, source hash records, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "CUDA", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, source hashes, relative paths, source backend names, intermediate CrossGL provenance, and file-granularity artifact source-map anchors. Report validation accepts generated source maps, records per-artifact source hash status and aggregate hash-status counts, and rejects malformed project source override metadata, source-map records, inconsistent source-map anchors, source hash records, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "HIP", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, source hashes, relative paths, source backend names, intermediate CrossGL provenance, and file-granularity artifact source-map anchors. Report validation accepts generated source maps, records per-artifact source hash status and aggregate hash-status counts, and rejects malformed project source override metadata, source-map records, inconsistent source-map anchors, source hash records, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "Mojo", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, source hashes, relative paths, source backend names, intermediate CrossGL provenance, and file-granularity artifact source-map anchors. Report validation accepts generated source maps, records per-artifact source hash status and aggregate hash-status counts, and rejects malformed project source override metadata, source-map records, inconsistent source-map anchors, source hash records, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "Rust", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, source hashes, relative paths, source backend names, intermediate CrossGL provenance, and file-granularity artifact source-map anchors. Report validation accepts generated source maps, records per-artifact source hash status and aggregate hash-status counts, and rejects malformed project source override metadata, source-map records, inconsistent source-map anchors, source hash records, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "Slang", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, source hashes, relative paths, source backend names, intermediate CrossGL provenance, and file-granularity artifact source-map anchors. Report validation accepts generated source maps, records per-artifact source hash status and aggregate hash-status counts, and rejects malformed project source override metadata, source-map records, inconsistent source-map anchors, source hash records, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "DirectX / HLSL", "project", "External corpus coverage", "partial", "Project reports can attach a repository-relative external corpus manifest, skip malformed manifest entries with structured diagnostics, and summarize declared valid paths, present and missing entries, discovered translation units, source-backend, target, and artifact rollups for manifest-backed project runs, and text inspection surfaces the same rollups. The pinned reduced-corpus manifest includes a focused DirectX reduction covered by backend fixture tests. This is a coverage and accounting layer only: CrossTL does not clone upstream repositories, run native build systems, or claim whole-corpus semantic parity from reduced backend fixtures."
   "OpenGL / GLSL", "project", "External corpus coverage", "partial", "Project reports can attach a repository-relative external corpus manifest, skip malformed manifest entries with structured diagnostics, and summarize declared valid paths, present and missing entries, discovered translation units, source-backend, target, and artifact rollups for manifest-backed project runs, and text inspection surfaces the same rollups. The pinned reduced-corpus manifest includes a focused OpenGL reduction covered by backend fixture tests. This is a coverage and accounting layer only: CrossTL does not clone upstream repositories, run native build systems, or claim whole-corpus semantic parity from reduced backend fixtures."
   "Metal", "project", "External corpus coverage", "partial", "Project reports can attach a repository-relative external corpus manifest, skip malformed manifest entries with structured diagnostics, and summarize declared valid paths, present and missing entries, discovered translation units, source-backend, target, and artifact rollups for manifest-backed project runs, and text inspection surfaces the same rollups. The pinned reduced-corpus manifest includes a focused Metal reduction covered by backend fixture tests. This is a coverage and accounting layer only: CrossTL does not clone upstream repositories, run native build systems, or claim whole-corpus semantic parity from reduced backend fixtures."
   "Vulkan SPIR-V", "project", "External corpus coverage", "partial", "Project reports can attach a repository-relative external corpus manifest, skip malformed manifest entries with structured diagnostics, and summarize declared valid paths, present and missing entries, discovered translation units, source-backend, target, and artifact rollups for manifest-backed project runs, and text inspection surfaces the same rollups. The current pinned reduced-corpus manifest covers DirectX, OpenGL, Metal, CUDA, and HIP reductions; this backend can participate in the backend-agnostic accounting layer when a matching manifest entry is added. CrossTL does not clone upstream repositories, run native build systems, or claim whole-corpus semantic parity from reduced backend fixtures."
   "CUDA", "project", "External corpus coverage", "partial", "Project reports can attach a repository-relative external corpus manifest, skip malformed manifest entries with structured diagnostics, and summarize declared valid paths, present and missing entries, discovered translation units, source-backend, target, and artifact rollups for manifest-backed project runs, and text inspection surfaces the same rollups. The pinned reduced-corpus manifest includes a focused CUDA reduction covered by backend fixture tests. This is a coverage and accounting layer only: CrossTL does not clone upstream repositories, run native build systems, or claim whole-corpus semantic parity from reduced backend fixtures."
   "HIP", "project", "External corpus coverage", "partial", "Project reports can attach a repository-relative external corpus manifest, skip malformed manifest entries with structured diagnostics, and summarize declared valid paths, present and missing entries, discovered translation units, source-backend, target, and artifact rollups for manifest-backed project runs, and text inspection surfaces the same rollups. The pinned reduced-corpus manifest includes a focused HIP reduction covered by backend fixture tests. This is a coverage and accounting layer only: CrossTL does not clone upstream repositories, run native build systems, or claim whole-corpus semantic parity from reduced backend fixtures."
   "Mojo", "project", "External corpus coverage", "partial", "Project reports can attach a repository-relative external corpus manifest, skip malformed manifest entries with structured diagnostics, and summarize declared valid paths, present and missing entries, discovered translation units, source-backend, target, and artifact rollups for manifest-backed project runs, and text inspection surfaces the same rollups. The current pinned reduced-corpus manifest covers DirectX, OpenGL, Metal, CUDA, and HIP reductions; this backend can participate in the backend-agnostic accounting layer when a matching manifest entry is added. CrossTL does not clone upstream repositories, run native build systems, or claim whole-corpus semantic parity from reduced backend fixtures."
   "Rust", "project", "External corpus coverage", "partial", "Project reports can attach a repository-relative external corpus manifest, skip malformed manifest entries with structured diagnostics, and summarize declared valid paths, present and missing entries, discovered translation units, source-backend, target, and artifact rollups for manifest-backed project runs, and text inspection surfaces the same rollups. The current pinned reduced-corpus manifest covers DirectX, OpenGL, Metal, CUDA, and HIP reductions; this backend can participate in the backend-agnostic accounting layer when a matching manifest entry is added. CrossTL does not clone upstream repositories, run native build systems, or claim whole-corpus semantic parity from reduced backend fixtures."
   "Slang", "project", "External corpus coverage", "partial", "Project reports can attach a repository-relative external corpus manifest, skip malformed manifest entries with structured diagnostics, and summarize declared valid paths, present and missing entries, discovered translation units, source-backend, target, and artifact rollups for manifest-backed project runs, and text inspection surfaces the same rollups. The pinned reduced-corpus manifest includes a focused Slang reduction covered by backend fixture tests. This is a coverage and accounting layer only: CrossTL does not clone upstream repositories, run native build systems, or claim whole-corpus semantic parity from reduced backend fixtures."

Documentation Sources
---------------------

The nightly documentation probe checks these URLs for reachability and
content hash changes. It does not overwrite feature statuses; status
changes must be reviewed and committed in the source catalog.

.. csv-table:: Official documentation URLs
   :header: "Backend", "Source", "URL"

   "DirectX / HLSL", "Microsoft Learn HLSL reference", "https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx-graphics-hlsl-reference"
   "DirectX / HLSL", "HLSL specification project", "https://microsoft.github.io/hlsl-specs/"
   "OpenGL / GLSL", "GLSL 4.60 specification", "https://raw.githubusercontent.com/KhronosGroup/OpenGL-Registry/main/specs/gl/GLSLangSpec.4.60.html"
   "OpenGL / GLSL", "OpenGL registry", "https://github.com/KhronosGroup/OpenGL-Registry"
   "Metal", "Apple Metal resources", "https://developer.apple.com/metal/resources/"
   "Metal", "Metal Shading Language specification", "https://developer.apple.com/metal/Metal-Shading-Language-Specification.pdf"
   "Vulkan SPIR-V", "SPIR-V unified grammar", "https://raw.githubusercontent.com/KhronosGroup/SPIRV-Headers/main/include/spirv/unified1/spirv.core.grammar.json"
   "Vulkan SPIR-V", "Khronos SPIR-V headers", "https://github.com/KhronosGroup/SPIRV-Headers"
   "CUDA", "CUDA C++ programming guide", "https://docs.nvidia.com/cuda/cuda-c-programming-guide/"
   "HIP", "ROCm HIP documentation", "https://rocm.docs.amd.com/projects/HIP/en/latest/"
   "Mojo", "Mojo manual", "https://mojolang.org/docs/manual/"
   "Rust", "Rust reference", "https://doc.rust-lang.org/reference/"
   "Slang", "Slang user guide", "https://shader-slang.org/slang/user-guide/"
