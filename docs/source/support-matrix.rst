Support Matrix
==============

.. This file is generated by tools/support_matrix.py. Do not edit by hand.

This matrix tracks CrossGL backend coverage from the checked-in support
catalog. A feature is marked supported only when the repo has implementation
and test evidence for the CrossGL contract. Unknown means unaudited, not
implicitly supported.

.. csv-table:: Status legend
   :header: "Code", "Status", "Meaning"
   :widths: 8, 20, 72

   "Y", "supported", "Implemented with positive tests for the repo-owned CrossGL contract."
   "P", "partial", "Implemented for common cases, but known target features or edge cases are missing."
   "D", "diagnostic", "The backend emits a deterministic diagnostic or fallback instead of full target code."
   "R", "validated_rejection", "The backend rejects this shape with an explicit validation error."
   "U", "unsupported", "The backend intentionally does not support this capability yet."
   "?", "unknown", "Not audited yet. Do not treat this as supported."

.. csv-table:: Backend inventory
   :header: "Backend", "Ext", "Target generator", "Native frontend", "Tests", "Test count", "Unsupported markers", "Docs source"

   "DirectX / HLSL", ".hlsl", "crosstl/translator/codegen/directx_codegen.py", "crosstl/backend/DirectX", "tests/test_translator/test_codegen/test_directx_codegen.py, tests/test_backend/test_directx", "998", "291", "Microsoft Learn HLSL reference; HLSL specification project"
   "OpenGL / GLSL", ".glsl", "crosstl/translator/codegen/GLSL_codegen.py", "crosstl/backend/GLSL", "tests/test_translator/test_codegen/test_GLSL_codegen.py, tests/test_backend/test_GLSL", "1087", "176", "GLSL 4.60 specification; OpenGL registry"
   "Metal", ".metal", "crosstl/translator/codegen/metal_codegen.py", "crosstl/backend/Metal", "tests/test_translator/test_codegen/test_metal_codegen.py, tests/test_backend/test_metal", "895", "488", "Apple Metal resources; Metal Shading Language specification"
   "Vulkan SPIR-V", ".spvasm", "crosstl/translator/codegen/SPIRV_codegen.py", "crosstl/backend/SPIRV", "tests/test_translator/test_codegen/test_SPIRV_codegen.py, tests/test_backend/test_SPIRV", "897", "34", "SPIR-V unified grammar; Khronos SPIR-V headers"
   "CUDA", ".cu", "crosstl/translator/codegen/cuda_codegen.py", "crosstl/backend/CUDA", "tests/test_translator/test_codegen/test_CUDA_codegen.py, tests/test_backend/test_CUDA", "676", "192", "CUDA C++ programming guide"
   "HIP", ".hip", "crosstl/translator/codegen/hip_codegen.py", "crosstl/backend/HIP", "tests/test_translator/test_codegen/test_hip_codegen.py, tests/test_backend/test_HIP", "732", "134", "ROCm HIP documentation"
   "Mojo", ".mojo", "crosstl/translator/codegen/mojo_codegen.py", "crosstl/backend/Mojo", "tests/test_translator/test_codegen/test_mojo_codegen.py, tests/test_backend/test_mojo", "785", "98", "Mojo manual"
   "Rust", ".rs", "crosstl/translator/codegen/rust_codegen.py", "crosstl/backend/Rust", "tests/test_translator/test_codegen/test_rust_codegen.py, tests/test_backend/test_rust", "853", "69", "Rust reference"
   "Slang", ".slang", "crosstl/translator/codegen/slang_codegen.py", "crosstl/backend/slang", "tests/test_translator/test_codegen/test_slang_codegen.py, tests/test_backend/test_slang", "728", "410", "Slang user guide"

.. csv-table:: Summary by backend
   :header: "Backend", "supported", "partial", "diagnostic", "validated_rejection", "unsupported", "unknown"

   "DirectX / HLSL", "49", "3", "2", "0", "0", "0"
   "OpenGL / GLSL", "49", "3", "2", "0", "0", "0"
   "Metal", "48", "3", "3", "0", "0", "0"
   "Vulkan SPIR-V", "49", "3", "2", "0", "0", "0"
   "CUDA", "44", "3", "7", "0", "0", "0"
   "HIP", "44", "3", "7", "0", "0", "0"
   "Mojo", "46", "3", "5", "0", "0", "0"
   "Rust", "47", "3", "4", "0", "0", "0"
   "Slang", "48", "3", "3", "0", "0", "0"

Graphics Backend Focus
----------------------

This view isolates the DirectX, OpenGL, and Metal rows that are in
scope for graphics backend completion work.

.. csv-table:: Graphics backend status summary
   :header: "Backend", "supported", "partial", "diagnostic", "validated_rejection", "unsupported", "unknown"

   "DirectX / HLSL", "49", "3", "2", "0", "0", "0"
   "OpenGL / GLSL", "49", "3", "2", "0", "0", "0"
   "Metal", "48", "3", "3", "0", "0", "0"

.. csv-table:: DirectX/OpenGL/Metal backlog
   :header: "Backend", "Category", "Feature", "Status", "Notes"

   "DirectX / HLSL", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, scans #include directives into per-unit includeDependencies records with local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes, source/include-dir resolution source, and project-define provenance for simple define-backed include targets, emits structured diagnostics for missing local, dynamic, and outside-project include directives with project-define provenance when applicable, validates include dependency record shape, summary rollups including source-backend, source-backend status, and resolution-source counts, missing include dependency summary rollups, current status, resolved path, resolved include hash, source/include-dir resolution source, and project-define include provenance, applies CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, source backend, and variant, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including variant rollups, missing include-path processing variant rollups, and text inspection identifies inactive include directories by path, include dependency status, kind, source-backend, source-backend status, resolution-source, and include-path processing rollups by status, source backend, and variant, resolved and unresolved include dependency samples with source-backend labels and project-define provenance, and sampled include-path processing artifact metadata. Include semantics remain backend-dependent, so this remains partial project support."
   "OpenGL / GLSL", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, scans #include directives into per-unit includeDependencies records with local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes, source/include-dir resolution source, and project-define provenance for simple define-backed include targets, emits structured diagnostics for missing local, dynamic, and outside-project include directives with project-define provenance when applicable, validates include dependency record shape, summary rollups including source-backend, source-backend status, and resolution-source counts, missing include dependency summary rollups, current status, resolved path, resolved include hash, source/include-dir resolution source, and project-define include provenance, applies CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, source backend, and variant, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including variant rollups, missing include-path processing variant rollups, and text inspection identifies inactive include directories by path, include dependency status, kind, source-backend, source-backend status, resolution-source, and include-path processing rollups by status, source backend, and variant, resolved and unresolved include dependency samples with source-backend labels and project-define provenance, and sampled include-path processing artifact metadata. Include semantics remain backend-dependent, so this remains partial project support."
   "Metal", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, scans #include directives into per-unit includeDependencies records with local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes, source/include-dir resolution source, and project-define provenance for simple define-backed include targets, emits structured diagnostics for missing local, dynamic, and outside-project include directives with project-define provenance when applicable, validates include dependency record shape, summary rollups including source-backend, source-backend status, and resolution-source counts, missing include dependency summary rollups, current status, resolved path, resolved include hash, source/include-dir resolution source, and project-define include provenance, applies CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, source backend, and variant, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including variant rollups, missing include-path processing variant rollups, and text inspection identifies inactive include directories by path, include dependency status, kind, source-backend, source-backend status, resolution-source, and include-path processing rollups by status, source backend, and variant, resolved and unresolved include dependency samples with source-backend labels and project-define provenance, and sampled include-path processing artifact metadata. Include semantics remain backend-dependent, so this remains partial project support."
   "DirectX / HLSL", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, inspection summaries and text output expose variant names and per-variant define counts without define values plus define-processing status, source-backend, and variant rollups plus sampled define-processing artifact metadata without define values, validates malformed define/variant metadata including empty mapping keys and forged variant define counts, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including variant rollups, missing define-processing variant rollups, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports API and CLI scoped runs for selected declared variants, de-duplicates repeated selections before artifact planning, and does not claim omitted variants as attempted. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "OpenGL / GLSL", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, inspection summaries and text output expose variant names and per-variant define counts without define values plus define-processing status, source-backend, and variant rollups plus sampled define-processing artifact metadata without define values, validates malformed define/variant metadata including empty mapping keys and forged variant define counts, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including variant rollups, missing define-processing variant rollups, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports API and CLI scoped runs for selected declared variants, de-duplicates repeated selections before artifact planning, and does not claim omitted variants as attempted. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "Metal", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, inspection summaries and text output expose variant names and per-variant define counts without define values plus define-processing status, source-backend, and variant rollups plus sampled define-processing artifact metadata without define values, validates malformed define/variant metadata including empty mapping keys and forged variant define counts, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including variant rollups, missing define-processing variant rollups, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports API and CLI scoped runs for selected declared variants, de-duplicates repeated selections before artifact planning, and does not claim omitted variants as attempted. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "DirectX / HLSL", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes, artifact source hashes, relative paths, source backend names, intermediate CrossGL provenance, file-granularity artifact source-map anchors, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, source-remap summary rollups by target, source backend, and variant, bounded source-map and source-remap artifact inspection samples, and text inspection derives file-level source-map counts from total and fine-grained source-map totals, artifact provenance summary rollups by pipeline, intermediate marker, and source backend with intermediate marker, bounded artifact provenance inspection samples, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and a file-level generated/original mapping for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, generated hash, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance, unit source hash records that are missing, malformed, or no longer match the current source file, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, source-map records, closed source-map field sets, non-empty source-map mappings, single file-level source-map mapping, source-map span consistency, source-map anchor consistency, current file-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, source-remap summary rollups including variant rollups, missing source-map and source-remap variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "OpenGL / GLSL", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes, artifact source hashes, relative paths, source backend names, intermediate CrossGL provenance, file-granularity artifact source-map anchors, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, source-remap summary rollups by target, source backend, and variant, bounded source-map and source-remap artifact inspection samples, and text inspection derives file-level source-map counts from total and fine-grained source-map totals, artifact provenance summary rollups by pipeline, intermediate marker, and source backend with intermediate marker, bounded artifact provenance inspection samples, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and a file-level generated/original mapping for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, generated hash, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance, unit source hash records that are missing, malformed, or no longer match the current source file, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, source-map records, closed source-map field sets, non-empty source-map mappings, single file-level source-map mapping, source-map span consistency, source-map anchor consistency, current file-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, source-remap summary rollups including variant rollups, missing source-map and source-remap variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "Metal", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes, artifact source hashes, relative paths, source backend names, intermediate CrossGL provenance, file-granularity artifact source-map anchors, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, source-remap summary rollups by target, source backend, and variant, bounded source-map and source-remap artifact inspection samples, and text inspection derives file-level source-map counts from total and fine-grained source-map totals, artifact provenance summary rollups by pipeline, intermediate marker, and source backend with intermediate marker, bounded artifact provenance inspection samples, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and a file-level generated/original mapping for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, generated hash, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance, unit source hash records that are missing, malformed, or no longer match the current source file, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, source-map records, closed source-map field sets, non-empty source-map mappings, single file-level source-map mapping, source-map span consistency, source-map anchor consistency, current file-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, source-remap summary rollups including variant rollups, missing source-map and source-remap variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."

Feature Matrix
--------------

Each category below uses the status codes from the legend.

.. csv-table:: target
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "CrossGL to target code generation", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: source
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Native source to CrossGL", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Native lexer coverage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Native preprocessor handling", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: stages
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Vertex stage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Fragment/pixel stage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Compute stage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Geometry stage", "Y", "Y", "D", "Y", "D", "D", "D", "D", "Y"
   "Tessellation stages", "Y", "D", "D", "Y", "D", "D", "D", "D", "Y"
   "Mesh/task/amplification stages", "Y", "Y", "Y", "Y", "D", "D", "D", "D", "D"
   "Ray tracing stages", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: stage I/O
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Stage parameter semantics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Direct function return semantics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Struct member semantics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: resources
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Explicit and automatic resource bindings", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Constant/uniform buffers", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Structured/storage buffers", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Resource arrays", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Texture and sampler object model", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "GLSL buffer block lowering", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Resource memory qualifiers", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: textures
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Texture sampling", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Texture LOD, gradient, and offset operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Projected texture operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Texture gather operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Shadow compare texture operations", "Y", "Y", "Y", "Y", "D", "D", "Y", "Y", "Y"
   "Texture query operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Texel fetch operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Multisample texture operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Advanced texture operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: images
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Storage image load/store", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Image atomics", "Y", "Y", "Y", "Y", "D", "D", "Y", "Y", "Y"
   "Multisample storage images", "D", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: language
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Struct declarations and construction", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Array declarations and access", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Function declarations and calls", "Y", "Y", "Y", "D", "D", "D", "D", "Y", "D"
   "Control flow", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Synchronization and memory barriers", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Wave/subgroup intrinsics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Match/pattern lowering", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Vector and matrix expressions", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Bitwise operations", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: validation
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Invalid shader shape validation", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

.. csv-table:: project
   :header: "Feature", "DirectX / HLSL", "OpenGL / GLSL", "Metal", "Vulkan SPIR-V", "CUDA", "HIP", "Mojo", "Rust", "Slang"

   "Repository source discovery", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Project include resolution", "P", "P", "P", "P", "P", "P", "P", "P", "P"
   "Macro and define variants", "P", "P", "P", "P", "P", "P", "P", "P", "P"
   "Batch project translation", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Optional validation hooks", "D", "D", "D", "D", "D", "D", "D", "D", "D"
   "Migration action report", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Project report inspection", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Artifact manifest", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "Source provenance and source-map bridge", "P", "P", "P", "P", "P", "P", "P", "P", "P"
   "Project diagnostics", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"
   "External corpus coverage", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y", "Y"

Backlog
-------

These rows are the current path to full backend coverage. Unknown rows
need an audit before implementation work can be scoped accurately.

.. csv-table:: Non-supported or unaudited feature rows
   :header: "Backend", "Category", "Feature", "Status", "Notes"

   "DirectX / HLSL", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, scans #include directives into per-unit includeDependencies records with local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes, source/include-dir resolution source, and project-define provenance for simple define-backed include targets, emits structured diagnostics for missing local, dynamic, and outside-project include directives with project-define provenance when applicable, validates include dependency record shape, summary rollups including source-backend, source-backend status, and resolution-source counts, missing include dependency summary rollups, current status, resolved path, resolved include hash, source/include-dir resolution source, and project-define include provenance, applies CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, source backend, and variant, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including variant rollups, missing include-path processing variant rollups, and text inspection identifies inactive include directories by path, include dependency status, kind, source-backend, source-backend status, resolution-source, and include-path processing rollups by status, source backend, and variant, resolved and unresolved include dependency samples with source-backend labels and project-define provenance, and sampled include-path processing artifact metadata. Include semantics remain backend-dependent, so this remains partial project support."
   "OpenGL / GLSL", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, scans #include directives into per-unit includeDependencies records with local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes, source/include-dir resolution source, and project-define provenance for simple define-backed include targets, emits structured diagnostics for missing local, dynamic, and outside-project include directives with project-define provenance when applicable, validates include dependency record shape, summary rollups including source-backend, source-backend status, and resolution-source counts, missing include dependency summary rollups, current status, resolved path, resolved include hash, source/include-dir resolution source, and project-define include provenance, applies CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, source backend, and variant, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including variant rollups, missing include-path processing variant rollups, and text inspection identifies inactive include directories by path, include dependency status, kind, source-backend, source-backend status, resolution-source, and include-path processing rollups by status, source backend, and variant, resolved and unresolved include dependency samples with source-backend labels and project-define provenance, and sampled include-path processing artifact metadata. Include semantics remain backend-dependent, so this remains partial project support."
   "Metal", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, scans #include directives into per-unit includeDependencies records with local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes, source/include-dir resolution source, and project-define provenance for simple define-backed include targets, emits structured diagnostics for missing local, dynamic, and outside-project include directives with project-define provenance when applicable, validates include dependency record shape, summary rollups including source-backend, source-backend status, and resolution-source counts, missing include dependency summary rollups, current status, resolved path, resolved include hash, source/include-dir resolution source, and project-define include provenance, applies CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, source backend, and variant, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including variant rollups, missing include-path processing variant rollups, and text inspection identifies inactive include directories by path, include dependency status, kind, source-backend, source-backend status, resolution-source, and include-path processing rollups by status, source backend, and variant, resolved and unresolved include dependency samples with source-backend labels and project-define provenance, and sampled include-path processing artifact metadata. Include semantics remain backend-dependent, so this remains partial project support."
   "Vulkan SPIR-V", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, scans #include directives into per-unit includeDependencies records with local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes, source/include-dir resolution source, and project-define provenance for simple define-backed include targets, emits structured diagnostics for missing local, dynamic, and outside-project include directives with project-define provenance when applicable, validates include dependency record shape, summary rollups including source-backend, source-backend status, and resolution-source counts, missing include dependency summary rollups, current status, resolved path, resolved include hash, source/include-dir resolution source, and project-define include provenance, applies CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, source backend, and variant, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including variant rollups, missing include-path processing variant rollups, and text inspection identifies inactive include directories by path, include dependency status, kind, source-backend, source-backend status, resolution-source, and include-path processing rollups by status, source backend, and variant, resolved and unresolved include dependency samples with source-backend labels and project-define provenance, and sampled include-path processing artifact metadata. Include semantics remain backend-dependent, so this remains partial project support."
   "CUDA", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, scans #include directives into per-unit includeDependencies records with local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes, source/include-dir resolution source, and project-define provenance for simple define-backed include targets, emits structured diagnostics for missing local, dynamic, and outside-project include directives with project-define provenance when applicable, validates include dependency record shape, summary rollups including source-backend, source-backend status, and resolution-source counts, missing include dependency summary rollups, current status, resolved path, resolved include hash, source/include-dir resolution source, and project-define include provenance, applies CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, source backend, and variant, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including variant rollups, missing include-path processing variant rollups, and text inspection identifies inactive include directories by path, include dependency status, kind, source-backend, source-backend status, resolution-source, and include-path processing rollups by status, source backend, and variant, resolved and unresolved include dependency samples with source-backend labels and project-define provenance, and sampled include-path processing artifact metadata. Include semantics remain backend-dependent, so this remains partial project support."
   "HIP", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, scans #include directives into per-unit includeDependencies records with local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes, source/include-dir resolution source, and project-define provenance for simple define-backed include targets, emits structured diagnostics for missing local, dynamic, and outside-project include directives with project-define provenance when applicable, validates include dependency record shape, summary rollups including source-backend, source-backend status, and resolution-source counts, missing include dependency summary rollups, current status, resolved path, resolved include hash, source/include-dir resolution source, and project-define include provenance, applies CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, source backend, and variant, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including variant rollups, missing include-path processing variant rollups, and text inspection identifies inactive include directories by path, include dependency status, kind, source-backend, source-backend status, resolution-source, and include-path processing rollups by status, source backend, and variant, resolved and unresolved include dependency samples with source-backend labels and project-define provenance, and sampled include-path processing artifact metadata. Include semantics remain backend-dependent, so this remains partial project support."
   "Mojo", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, scans #include directives into per-unit includeDependencies records with local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes, source/include-dir resolution source, and project-define provenance for simple define-backed include targets, emits structured diagnostics for missing local, dynamic, and outside-project include directives with project-define provenance when applicable, validates include dependency record shape, summary rollups including source-backend, source-backend status, and resolution-source counts, missing include dependency summary rollups, current status, resolved path, resolved include hash, source/include-dir resolution source, and project-define include provenance, applies CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, source backend, and variant, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including variant rollups, missing include-path processing variant rollups, and text inspection identifies inactive include directories by path, include dependency status, kind, source-backend, source-backend status, resolution-source, and include-path processing rollups by status, source backend, and variant, resolved and unresolved include dependency samples with source-backend labels and project-define provenance, and sampled include-path processing artifact metadata. Include semantics remain backend-dependent, so this remains partial project support."
   "Rust", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, scans #include directives into per-unit includeDependencies records with local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes, source/include-dir resolution source, and project-define provenance for simple define-backed include targets, emits structured diagnostics for missing local, dynamic, and outside-project include directives with project-define provenance when applicable, validates include dependency record shape, summary rollups including source-backend, source-backend status, and resolution-source counts, missing include dependency summary rollups, current status, resolved path, resolved include hash, source/include-dir resolution source, and project-define include provenance, applies CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, source backend, and variant, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including variant rollups, missing include-path processing variant rollups, and text inspection identifies inactive include directories by path, include dependency status, kind, source-backend, source-backend status, resolution-source, and include-path processing rollups by status, source backend, and variant, resolved and unresolved include dependency samples with source-backend labels and project-define provenance, and sampled include-path processing artifact metadata. Include semantics remain backend-dependent, so this remains partial project support."
   "Slang", "project", "Project include resolution", "partial", "The project pipeline records include directories, records per-include directory status records and status counts, reports missing include directories, reports include entries that resolve to files or other non-directory paths, reports include directories that resolve outside the repository, scans #include directives into per-unit includeDependencies records with local, system, or dynamic kind, line and column, resolved, missing, system, dynamic, or outside-project status, repository-relative resolved paths, resolved include SHA-256 hashes, source/include-dir resolution source, and project-define provenance for simple define-backed include targets, emits structured diagnostics for missing local, dynamic, and outside-project include directives with project-define provenance when applicable, validates include dependency record shape, summary rollups including source-backend, source-backend status, and resolution-source counts, missing include dependency summary rollups, current status, resolved path, resolved include hash, source/include-dir resolution source, and project-define include provenance, applies CLI include-directory overrides, passes only active existing repository-contained resolved paths to source frontends that expose include-path preprocessor options, records each artifact include-path processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when active include paths cannot be forwarded by a source frontend, and includes include-path processing rollups by status, source backend, and variant, validates malformed or forged artifact include-path processing metadata including unexpected generated processing fields and include-path processing summary rollup mismatches including variant rollups, missing include-path processing variant rollups, and text inspection identifies inactive include directories by path, include dependency status, kind, source-backend, source-backend status, resolution-source, and include-path processing rollups by status, source backend, and variant, resolved and unresolved include dependency samples with source-backend labels and project-define provenance, and sampled include-path processing artifact metadata. Include semantics remain backend-dependent, so this remains partial project support."
   "DirectX / HLSL", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, inspection summaries and text output expose variant names and per-variant define counts without define values plus define-processing status, source-backend, and variant rollups plus sampled define-processing artifact metadata without define values, validates malformed define/variant metadata including empty mapping keys and forged variant define counts, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including variant rollups, missing define-processing variant rollups, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports API and CLI scoped runs for selected declared variants, de-duplicates repeated selections before artifact planning, and does not claim omitted variants as attempted. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "OpenGL / GLSL", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, inspection summaries and text output expose variant names and per-variant define counts without define values plus define-processing status, source-backend, and variant rollups plus sampled define-processing artifact metadata without define values, validates malformed define/variant metadata including empty mapping keys and forged variant define counts, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including variant rollups, missing define-processing variant rollups, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports API and CLI scoped runs for selected declared variants, de-duplicates repeated selections before artifact planning, and does not claim omitted variants as attempted. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "Metal", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, inspection summaries and text output expose variant names and per-variant define counts without define values plus define-processing status, source-backend, and variant rollups plus sampled define-processing artifact metadata without define values, validates malformed define/variant metadata including empty mapping keys and forged variant define counts, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including variant rollups, missing define-processing variant rollups, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports API and CLI scoped runs for selected declared variants, de-duplicates repeated selections before artifact planning, and does not claim omitted variants as attempted. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "Vulkan SPIR-V", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, inspection summaries and text output expose variant names and per-variant define counts without define values plus define-processing status, source-backend, and variant rollups plus sampled define-processing artifact metadata without define values, validates malformed define/variant metadata including empty mapping keys and forged variant define counts, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including variant rollups, missing define-processing variant rollups, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports API and CLI scoped runs for selected declared variants, de-duplicates repeated selections before artifact planning, and does not claim omitted variants as attempted. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "CUDA", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, inspection summaries and text output expose variant names and per-variant define counts without define values plus define-processing status, source-backend, and variant rollups plus sampled define-processing artifact metadata without define values, validates malformed define/variant metadata including empty mapping keys and forged variant define counts, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including variant rollups, missing define-processing variant rollups, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports API and CLI scoped runs for selected declared variants, de-duplicates repeated selections before artifact planning, and does not claim omitted variants as attempted. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "HIP", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, inspection summaries and text output expose variant names and per-variant define counts without define values plus define-processing status, source-backend, and variant rollups plus sampled define-processing artifact metadata without define values, validates malformed define/variant metadata including empty mapping keys and forged variant define counts, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including variant rollups, missing define-processing variant rollups, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports API and CLI scoped runs for selected declared variants, de-duplicates repeated selections before artifact planning, and does not claim omitted variants as attempted. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "Mojo", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, inspection summaries and text output expose variant names and per-variant define counts without define values plus define-processing status, source-backend, and variant rollups plus sampled define-processing artifact metadata without define values, validates malformed define/variant metadata including empty mapping keys and forged variant define counts, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including variant rollups, missing define-processing variant rollups, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports API and CLI scoped runs for selected declared variants, de-duplicates repeated selections before artifact planning, and does not claim omitted variants as attempted. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "Rust", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, inspection summaries and text output expose variant names and per-variant define counts without define values plus define-processing status, source-backend, and variant rollups plus sampled define-processing artifact metadata without define values, validates malformed define/variant metadata including empty mapping keys and forged variant define counts, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including variant rollups, missing define-processing variant rollups, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports API and CLI scoped runs for selected declared variants, de-duplicates repeated selections before artifact planning, and does not claim omitted variants as attempted. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "Slang", "project", "Macro and define variants", "partial", "The project pipeline records define sets and named variant metadata in portability reports, preserves define and variant maps alongside counts and per-variant define counts, records report CLI variant metadata, records each artifact applied define map and define-processing status (forwarded, not-requested, or not-supported) with source frontend support metadata, emits warning diagnostics and missing-capability rollups when configured defines cannot be forwarded by a source frontend, inspection summaries and text output expose variant names and per-variant define counts without define values plus define-processing status, source-backend, and variant rollups plus sampled define-processing artifact metadata without define values, validates malformed define/variant metadata including empty mapping keys and forged variant define counts, undeclared artifact and validation-record variants, missing artifact define maps, artifact define maps that do not match base defines merged with the artifact variant, malformed or forged artifact define-processing metadata including unexpected generated processing fields, and define-processing summary rollup mismatches including variant rollups, missing define-processing variant rollups, rejects malformed define and variant TOML values before scan, applies CLI define overrides, passes defines to source frontends that expose preprocessor options, applies object-like define expansion and #if/#ifdef/#ifndef/#elif/#else/#endif branch selection in the CrossGL source frontend when defines are provided, and expands configured named variants into separate artifact attempts with base defines merged with variant overrides, and supports API and CLI scoped runs for selected declared variants, de-duplicates repeated selections before artifact planning, and does not claim omitted variants as attempted. Native preprocessor semantics remain backend-dependent, so this remains partial project support."
   "DirectX / HLSL", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes, artifact source hashes, relative paths, source backend names, intermediate CrossGL provenance, file-granularity artifact source-map anchors, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, source-remap summary rollups by target, source backend, and variant, bounded source-map and source-remap artifact inspection samples, and text inspection derives file-level source-map counts from total and fine-grained source-map totals, artifact provenance summary rollups by pipeline, intermediate marker, and source backend with intermediate marker, bounded artifact provenance inspection samples, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and a file-level generated/original mapping for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, generated hash, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance, unit source hash records that are missing, malformed, or no longer match the current source file, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, source-map records, closed source-map field sets, non-empty source-map mappings, single file-level source-map mapping, source-map span consistency, source-map anchor consistency, current file-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, source-remap summary rollups including variant rollups, missing source-map and source-remap variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "OpenGL / GLSL", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes, artifact source hashes, relative paths, source backend names, intermediate CrossGL provenance, file-granularity artifact source-map anchors, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, source-remap summary rollups by target, source backend, and variant, bounded source-map and source-remap artifact inspection samples, and text inspection derives file-level source-map counts from total and fine-grained source-map totals, artifact provenance summary rollups by pipeline, intermediate marker, and source backend with intermediate marker, bounded artifact provenance inspection samples, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and a file-level generated/original mapping for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, generated hash, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance, unit source hash records that are missing, malformed, or no longer match the current source file, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, source-map records, closed source-map field sets, non-empty source-map mappings, single file-level source-map mapping, source-map span consistency, source-map anchor consistency, current file-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, source-remap summary rollups including variant rollups, missing source-map and source-remap variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "Metal", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes, artifact source hashes, relative paths, source backend names, intermediate CrossGL provenance, file-granularity artifact source-map anchors, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, source-remap summary rollups by target, source backend, and variant, bounded source-map and source-remap artifact inspection samples, and text inspection derives file-level source-map counts from total and fine-grained source-map totals, artifact provenance summary rollups by pipeline, intermediate marker, and source backend with intermediate marker, bounded artifact provenance inspection samples, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and a file-level generated/original mapping for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, generated hash, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance, unit source hash records that are missing, malformed, or no longer match the current source file, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, source-map records, closed source-map field sets, non-empty source-map mappings, single file-level source-map mapping, source-map span consistency, source-map anchor consistency, current file-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, source-remap summary rollups including variant rollups, missing source-map and source-remap variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "Vulkan SPIR-V", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes, artifact source hashes, relative paths, source backend names, intermediate CrossGL provenance, file-granularity artifact source-map anchors, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, source-remap summary rollups by target, source backend, and variant, bounded source-map and source-remap artifact inspection samples, and text inspection derives file-level source-map counts from total and fine-grained source-map totals, artifact provenance summary rollups by pipeline, intermediate marker, and source backend with intermediate marker, bounded artifact provenance inspection samples, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and a file-level generated/original mapping for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, generated hash, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance, unit source hash records that are missing, malformed, or no longer match the current source file, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, source-map records, closed source-map field sets, non-empty source-map mappings, single file-level source-map mapping, source-map span consistency, source-map anchor consistency, current file-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, source-remap summary rollups including variant rollups, missing source-map and source-remap variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "CUDA", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes, artifact source hashes, relative paths, source backend names, intermediate CrossGL provenance, file-granularity artifact source-map anchors, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, source-remap summary rollups by target, source backend, and variant, bounded source-map and source-remap artifact inspection samples, and text inspection derives file-level source-map counts from total and fine-grained source-map totals, artifact provenance summary rollups by pipeline, intermediate marker, and source backend with intermediate marker, bounded artifact provenance inspection samples, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and a file-level generated/original mapping for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, generated hash, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance, unit source hash records that are missing, malformed, or no longer match the current source file, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, source-map records, closed source-map field sets, non-empty source-map mappings, single file-level source-map mapping, source-map span consistency, source-map anchor consistency, current file-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, source-remap summary rollups including variant rollups, missing source-map and source-remap variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "HIP", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes, artifact source hashes, relative paths, source backend names, intermediate CrossGL provenance, file-granularity artifact source-map anchors, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, source-remap summary rollups by target, source backend, and variant, bounded source-map and source-remap artifact inspection samples, and text inspection derives file-level source-map counts from total and fine-grained source-map totals, artifact provenance summary rollups by pipeline, intermediate marker, and source backend with intermediate marker, bounded artifact provenance inspection samples, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and a file-level generated/original mapping for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, generated hash, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance, unit source hash records that are missing, malformed, or no longer match the current source file, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, source-map records, closed source-map field sets, non-empty source-map mappings, single file-level source-map mapping, source-map span consistency, source-map anchor consistency, current file-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, source-remap summary rollups including variant rollups, missing source-map and source-remap variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "Mojo", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes, artifact source hashes, relative paths, source backend names, intermediate CrossGL provenance, file-granularity artifact source-map anchors, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, source-remap summary rollups by target, source backend, and variant, bounded source-map and source-remap artifact inspection samples, and text inspection derives file-level source-map counts from total and fine-grained source-map totals, artifact provenance summary rollups by pipeline, intermediate marker, and source backend with intermediate marker, bounded artifact provenance inspection samples, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and a file-level generated/original mapping for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, generated hash, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance, unit source hash records that are missing, malformed, or no longer match the current source file, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, source-map records, closed source-map field sets, non-empty source-map mappings, single file-level source-map mapping, source-map span consistency, source-map anchor consistency, current file-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, source-remap summary rollups including variant rollups, missing source-map and source-remap variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "Rust", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes, artifact source hashes, relative paths, source backend names, intermediate CrossGL provenance, file-granularity artifact source-map anchors, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, source-remap summary rollups by target, source backend, and variant, bounded source-map and source-remap artifact inspection samples, and text inspection derives file-level source-map counts from total and fine-grained source-map totals, artifact provenance summary rollups by pipeline, intermediate marker, and source backend with intermediate marker, bounded artifact provenance inspection samples, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and a file-level generated/original mapping for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, generated hash, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance, unit source hash records that are missing, malformed, or no longer match the current source file, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, source-map records, closed source-map field sets, non-empty source-map mappings, single file-level source-map mapping, source-map span consistency, source-map anchor consistency, current file-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, source-remap summary rollups including variant rollups, missing source-map and source-remap variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."
   "Slang", "project", "Source provenance and source-map bridge", "partial", "Project reports include configured source override maps, unit source hashes, artifact source hashes, relative paths, source backend names, intermediate CrossGL provenance, file-granularity artifact source-map anchors, compiler source-remap sidecar metadata for generated CrossGL artifacts, source-map summary rollups by granularity, target, source backend, and variant, source-remap summary rollups by target, source backend, and variant, bounded source-map and source-remap artifact inspection samples, and text inspection derives file-level source-map counts from total and fine-grained source-map totals, artifact provenance summary rollups by pipeline, intermediate marker, and source backend with intermediate marker, bounded artifact provenance inspection samples, plus unit and skipped source override rollups. CrossGL target artifacts write schema-1 source-remap sidecars with generatedFile and a file-level generated/original mapping for compiler --source-remap consumers. Report validation accepts generated source maps and source remaps, records per-artifact source hash, generated hash, source-map, and source-remap statuses, includes non-OK status details in failed-artifact text, and records aggregate validation status counts, and rejects malformed project config/source override metadata, invalid project config paths, inconsistent unit and skipped source override provenance, unit source hash records that are missing, malformed, or no longer match the current source file, artifact source paths that are not repository-relative including drive-qualified Windows paths, artifact output paths that are outside project.outputDir or do not match the source-relative target/variant layout, artifact records whose sources are not declared translation units, unregistered or non-canonical unit source backend names, artifact source backend names that do not match declared units, source-map records, closed source-map field sets, non-empty source-map mappings, single file-level source-map mapping, source-map span consistency, source-map anchor consistency, current file-level source-map span coverage, source-map summary rollups including variant rollups, source-remap records, source-remap summary rollups including variant rollups, missing source-map and source-remap variant rollups, missing or malformed source-remap metadata for CrossGL artifacts, stale source-remap hashes, source-remap content mismatches, compiler-compatible source-remap sidecar semantics, closed compiler source-remap sidecar field sets, closed generated hash, artifact provenance, and source-remap field sets, artifact source hash records, artifact source hashes that do not match declared translation-unit source hashes, source artifacts whose current file hash does not match the report, missing source artifacts with recorded hashes, missing artifact provenance summary rollups, missing provenance in full reports, forged artifact pipeline provenance, intermediate provenance that does not match source and target backends, and provenance records before artifact validation. Fine-grained source maps are not implemented in the translator project pipeline yet."

Documentation Sources
---------------------

The nightly documentation probe checks these URLs for reachability and
content hash changes. It does not overwrite feature statuses; status
changes must be reviewed and committed in the source catalog.

.. csv-table:: Official documentation URLs
   :header: "Backend", "Source", "URL"

   "DirectX / HLSL", "Microsoft Learn HLSL reference", "https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx-graphics-hlsl-reference"
   "DirectX / HLSL", "HLSL specification project", "https://microsoft.github.io/hlsl-specs/"
   "OpenGL / GLSL", "GLSL 4.60 specification", "https://raw.githubusercontent.com/KhronosGroup/OpenGL-Registry/main/specs/gl/GLSLangSpec.4.60.html"
   "OpenGL / GLSL", "OpenGL registry", "https://github.com/KhronosGroup/OpenGL-Registry"
   "Metal", "Apple Metal resources", "https://developer.apple.com/metal/resources/"
   "Metal", "Metal Shading Language specification", "https://developer.apple.com/metal/Metal-Shading-Language-Specification.pdf"
   "Vulkan SPIR-V", "SPIR-V unified grammar", "https://raw.githubusercontent.com/KhronosGroup/SPIRV-Headers/main/include/spirv/unified1/spirv.core.grammar.json"
   "Vulkan SPIR-V", "Khronos SPIR-V headers", "https://github.com/KhronosGroup/SPIRV-Headers"
   "CUDA", "CUDA C++ programming guide", "https://docs.nvidia.com/cuda/cuda-c-programming-guide/"
   "HIP", "ROCm HIP documentation", "https://rocm.docs.amd.com/projects/HIP/en/latest/"
   "Mojo", "Mojo manual", "https://mojolang.org/docs/manual/"
   "Rust", "Rust reference", "https://doc.rust-lang.org/reference/"
   "Slang", "Slang user guide", "https://shader-slang.org/slang/user-guide/"
