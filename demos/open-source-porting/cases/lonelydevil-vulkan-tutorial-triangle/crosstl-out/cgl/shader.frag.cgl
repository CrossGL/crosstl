#version 450

shader main {
    struct FragmentInput {
    vec3 fragColor @location(0);
    };

    fragment {
    vec4 main(FragmentInput input) @location(0) @ outColor {
        vec4 outColor;
        outColor = vec4(input.fragColor, 1.0);
        return outColor;
    }
    }
}
