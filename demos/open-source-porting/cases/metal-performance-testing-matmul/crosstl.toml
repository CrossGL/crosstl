[project]
source_roots = ["."]
include = ["mat_mul_simple1.metal"]
include_dirs = ["."]
targets = ["cgl", "opengl", "metal", "directx"]
output_dir = "crosstl-out"
external_corpus_manifest = "corpus.json"
