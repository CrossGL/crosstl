[project]
source_roots = ["."]
include = ["triangle.vert", "triangle.frag"]
targets = ["cgl", "opengl", "metal", "directx", "vulkan"]
output_dir = "crosstl-out"
external_corpus_manifest = "corpus.json"
