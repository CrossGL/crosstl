[project]
source_roots = ["."]
include = ["rectangle.vert.glsl", "rectangle.frag.glsl"]
targets = ["cgl", "metal", "opengl", "directx", "vulkan"]
output_dir = "crosstl-out"
external_corpus_manifest = "corpus.json"
