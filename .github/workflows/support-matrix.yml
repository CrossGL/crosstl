name: Support Matrix

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: "17 3 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate support matrix
        run: python tools/support_matrix.py check --output support/generated/support-matrix-check.json

      - name: Upload support matrix check report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: support-matrix-check-report
          path: support/generated/support-matrix-check.json
          retention-days: 30

  docs-probe:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Probe backend documentation sources
        run: python tools/support_matrix.py docs --output support/generated/backend-docs-report.json

      - name: Upload documentation probe report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-docs-report
          path: support/generated/backend-docs-report.json
          retention-days: 30
